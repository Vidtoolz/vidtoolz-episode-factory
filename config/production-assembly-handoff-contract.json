{
  "schema": "vidtoolz.productionAssemblyHandoffContract.v1",
  "status": "PROPOSED_NOT_GATE_AUTHORITY",
  "architecture_outcome": "C",
  "statement": "The canonical Production composition engine supports mode-conditioned visual drafts, presenter-validation drafts, and final-performance assembly. Renderer evidence remains non-gating.",
  "draft_classes": {
    "VISUAL_DRAFT": "Requires approved Story, script-beat narration alignment, composition, assets, music policy and output identity; presenter media and human-performance review are forbidden when no presenter layer exists.",
    "PRESENTER_VALIDATION_DRAFT": "Supports mixed visual-only and presenter beats; exact human presenter authority is required when any visible presenter layer is requested.",
    "FINAL_PERFORMANCE_ASSEMBLY": "Requires FINAL_HUMAN_PERFORMANCE and production-capture authority."
  },
  "authoritative_inputs": [
    "exact approved Story project/version/content hash",
    "exact approved Visual Plan V2 identity/digest",
    "either script-beat narration alignment or mode-appropriate human-performance authority",
    "explicitly bound approved insert assets",
    "explicitly bound selected music identity",
    "explicit timeline/edit plan identity and digest"
  ],
  "forbidden_inputs": [
    "PROXY_PRESENTER",
    "Draft V1 as presenter source",
    "stale Visual Plan V1",
    "unapproved or unbound inserts",
    "latest-file discovery",
    "provisional or inferred presenter segment boundaries when presenter authority is required",
    "a historical KEEP ALL review used as boundary approval"
  ],
  "output_class": "PRODUCTION_ASSEMBLY_CANDIDATE",
  "evidence_class": "PROPOSED_PRODUCTION_ASSEMBLY_TECHNICAL_EVIDENCE",
  "evidence_may_claim": [
    "the exact declared narration or selected human source intervals were consumed",
    "the output bytes decode and match their recorded SHA-256",
    "the output is bound to the declared Story, Visual Plan, sources, inserts, music, timeline and toolchain"
  ],
  "evidence_must_not_claim": [
    "final creative approval",
    "final mix approval",
    "color approval",
    "publication readiness",
    "Gate 9 completion",
    "Gate 10 approval"
  ],
  "claude_boundary": {
    "may": ["implement or operate the technical assembly lane against exact eligible inputs", "emit a hash-bound candidate and technical evidence"],
    "may_not": ["infer section boundaries", "select performances", "substitute proxy or synthetic media", "reuse DRAFT_ASSEMBLY as Production authority", "claim a human gate"]
  },
  "hermes_boundary": {
    "may": ["materialize already-authoritative bindings", "route the exact handoff", "present the candidate for Mikko review"],
    "may_not": ["infer boundaries", "select a take", "approve the edit", "write final-review PASS"]
  },
  "human_gate": "Presenter boundaries and selections require human authority only when presenter media is requested. A VISUAL_DRAFT review may evaluate story, visuals, pacing, music and edit logic but cannot approve presenter performance, camera framing, keying, publication, or a final gate.",
  "deterministic_renderer_closure": {
    "canonical_implementation": "scripts/production-assembly-renderer.js",
    "other_supported_production_renderers": [],
    "entrypoint": "node scripts/production-assembly-renderer.js --spec <production-assembly-render-spec.json>",
    "composition_helper": "scripts/production-assembly-composition.js (internal module; not an entry point)",
    "v2_composition_schema": "vidtoolz.productionAssemblyComposition.v1",
    "v2_asset_manifest_schema": "vidtoolz.productionAssemblyAssetManifest.v1",
    "preview_entrypoint": "node scripts/production-assembly-renderer.js --spec <production-assembly-render-spec.json> --beat <beat-id> --preview-output <relative-preview.mp4>",
    "sequence": [
      "validate exact release packet, draft class, Story, visual plan, narration or presenter authority, source hashes, composition assets and human-authorized music policy",
      "acquire an owner-identity render lock and conservatively recover only a provably dead stable holder",
      "persist a digest-bound deterministic render plan",
      "render only to a plan-specific partial path",
      "run stream, cadence, frame-count, duration, source-coverage, insert and full-decode machine QC",
      "emit renderer manifest and non-gating technical evidence",
      "move the candidate into place and write the COMPLETE marker last"
    ],
    "completion_authority": "A candidate without the matching .complete.json marker is incomplete even if MP4 bytes exist.",
    "recovery": "Retry the identical spec and plan; an exact QC-valid staged render may resume at attestation/finalization, exact completed output is reused, and conflicting or changing output fails closed.",
    "overwrite_policy": "A COMPLETE candidate is immutable. The identical plan reuses it; a different plan requires a new output/candidate identity.",
    "lock_identity": ["render ID", "PID", "process start identity", "hostname", "start timestamp", "handoff digest", "plan digest", "intended output"],
    "independent_qc": "Hermes/QC remains a separate attester after renderer closure; renderer evidence does not replace independent QC or human review.",
    "music_authority": "Music policy uses an append-only digest-bound succession chain. The latest sole ACTIVE decision must be HUMAN and agree with the release packet; AGENT_DEFAULT may remain historical but is not durable creative authority.",
    "performance_roles": {
      "HUMAN_DRAFT_PERFORMANCE": "May consume selected PROOF_CAPTURE or PRODUCTION_CAPTURE sources; output remains a human draft candidate.",
      "FINAL_HUMAN_PERFORMANCE": "Requires selected PRODUCTION_CAPTURE sources; renderer success still does not confer final creative or gate approval."
    },
    "narration_roles": {
      "SYNTHETIC_DRAFT_NARRATION": "Legal only as declared draft narration with a SHA-bound script-beat alignment; it does not imply presenter media or human performance.",
      "HUMAN_DRAFT_NARRATION": "May supply draft timing and audio without implying presenter imagery or presenter-boundary approval."
    }
  }
}
