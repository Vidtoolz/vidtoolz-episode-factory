{
  "schema": "vidtoolz.productionAssemblyCompositionContract.v1",
  "authority": "execution contract only; never creative, Gate 9, Gate 10, or publication authority",
  "canonical_entrypoint": "scripts/production-assembly-renderer.js",
  "composition_schema": "vidtoolz.productionAssemblyComposition.v1",
  "asset_manifest_schema": "vidtoolz.productionAssemblyAssetManifest.v1",
  "timeline": {
    "unit": "integer milliseconds",
    "coverage": "FULL_PROGRAMME",
    "beat_rule": "each beat is wholly inside one HUMAN_CONFIRMED or CAPTURE_EVENT_BOUND section; beats are contiguous and non-overlapping",
    "audio_rule": "confirmed section audio is assembled once; visual beats never restart or redefine human performance"
  },
  "primary_owners": ["GENERATED_VISUAL", "PRESENTER", "TYPOGRAPHY"],
  "layer_types": ["FULL_CANVAS_VISUAL", "PRESENTER", "TYPOGRAPHY"],
  "invariants": [
    "exactly one primary owner per beat",
    "z-order is explicit and unique",
    "presenter absence is first-class",
    "no raw ffmpeg filter text is accepted",
    "duplicate full-frame assets and duplicate semantic typography are rejected",
    "all assets are path-confined and SHA-256 bound",
    "composition data participates in the frozen render-plan digest"
  ],
  "presenter_geometry": {
    "required": ["x", "y", "width", "height", "anchor"],
    "anchors": ["TOP_LEFT", "TOP_RIGHT", "BOTTOM_LEFT", "BOTTOM_RIGHT", "CENTER"],
    "bleed": ["LEFT", "RIGHT", "BOTTOM"],
    "edge_treatments": ["NONE", "FEATHER_INNER"],
    "ramp_curves": ["LINEAR", "SMOOTH"]
  },
  "visual_media": {
    "roles": ["GENERATED_VIDEO", "DETERMINISTIC_MOTION_GRAPHIC", "TYPOGRAPHIC", "STATIC_GENERATED_IMAGE_WITH_MOTION", "PRESENTER_ONLY", "EXISTING_APPROVED_ASSET"],
    "fit": ["COVER", "CONTAIN"],
    "duration_policy": ["TRIM", "LOOP_EXPLICIT", "STILL"],
    "still_motion": ["STATIC", "SLOW_SCALE", "PAN", "ZOOM"],
    "presenter_matte_format_for_current_run": "VP9-alpha WebM decoded explicitly with libvpx-vp9",
    "presenter_matte_alternative": "ProRes 4444 remains supported where already available but is not required for the current run",
    "presenter_matte_rule": "PRESENTER_ONLY derivatives require explicit alpha semantics, meaningful-alpha validation, a bounded derivative visual crop, immutable source-master lineage, and explicit source-time mapping; automatic decoder selection and extension-only dispatch are forbidden"
  },
  "fallback_policies": ["REQUIRED", "FALLBACK_ALLOWED", "OPTIONAL"],
  "music_policies": ["FADE_EARLY", "LOOP_WITH_CROSSFADE", "FULL_PROGRAMME", "NONE"],
  "asset_manifest_required_fields": [
    "asset_id",
    "role",
    "path",
    "sha256",
    "media_kind",
    "width",
    "height",
    "duration_ms where applicable",
    "alpha for PRESENTER_ONLY derivatives",
    "visual_crop for PRESENTER_ONLY derivatives",
    "provenance",
    "status",
    "policy",
    "intended_beat_ids"
  ],
  "preview": {
    "entrypoint": "node scripts/production-assembly-renderer.js --spec <spec.json> --beat <beat-id> --preview-output <relative-output.mp4>",
    "engine": "same frozen plan and ffmpeg composition graph as full render",
    "authority": "NON_AUTHORITATIVE_ENGINEERING_PREVIEW",
    "complete_marker": false
  }
}
