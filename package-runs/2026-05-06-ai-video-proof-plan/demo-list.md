# Demo List

| demo | what it proves | setup needed | status |
| --- | --- | --- | --- |
| What will this video prove on screen? | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| Here is the working example for this video. | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| The pasted transcript matters because it did not produce the weak example I had prepared. It gave more practical workflow ideas and even avoided generic AI-tools listicles. | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| So I am not going to pretend ChatGPT generated this weak example. I am going to label it as a prepared working example: | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| The reproduced ChatGPT capture can demonstrate the proof-plan workflow, but it is not original transcript evidence. The original ChatGPT UI output from `capture-transcript.md` is unavailable as visual proof, so any script, narration, or graphic using capture 6 must label it as reproduced or controlled evidence. | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| That still helps, because it shows the point in a different way: you cannot script around the AI output you expected. You have to check what the tool actually gives you, then decide what evidence the video can honestly use. | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| But before you script it, you need to ask what the video will actually prove. | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
| Will it prove that these tools improve a real workflow? | Shows the viewer the workflow instead of only describing it. | Prepare source files, tabs, and clean state. | TODO |
