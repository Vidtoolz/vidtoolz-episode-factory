# CANARY — NOT FOR PUBLICATION — RETAIN FOR ARCHITECTURE EVIDENCE

This is a **genuine package run**, created through the real production path
(`scripts/package-engine-new-run.js`). It is the first genuine package run in
this repository, and it exists to prove that the canonical lifecycle and its
projections behave consistently against one real run rather than only against
fixtures.

**Do not publish. Do not upload. Do not treat as creative work.**

## What it earned

- `package-selection` — **complete**, via the real operator selection contract.
- `research` — reached and **held at HUMAN_REVIEW_REQUIRED**. Evidence is
  complete (3 sources, 3 proof items, 2 objections) and the sufficiency review
  reports `READY FOR RESEARCH REVIEW`. The only remaining blocker is the exact
  `Research approval: PASS` marker, which is **Mikko's authority alone** and was
  deliberately not written.

## Why it stops there

Stopping is the successful outcome. A lifecycle that could be advanced past a
human gate by an agent would be the defect.

## Retention

Retained as architecture evidence and **parked**, not completed. It never earned
completion and must never be marked as having done so.

Full evidence: `package-runs/2026-08-25-real-lifecycle-integration-proof/`.
