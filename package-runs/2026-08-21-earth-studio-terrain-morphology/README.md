# Terrain morphology production evidence

Human authority: `472a4287cd76e33a53cff8f218147bd77d0fa170`.

This package proves the deterministic morphology policy and preserves all historical evidence. The four calibration shots reproduce Mikko's selected angles; four unseen cases exercise semantic generalization and the terrain-floor clamp. Grand Canyon's natural region-scale grammar remains a reveal; its 74° policy is demonstrated with an explicit orbit-family terrain shot rather than silently changing movement grammar.

Real Google Earth Studio imported all 8/8 generated projects. Authenticated scene-model readback matched every authored tilt, found zero roll, and found zero terminal pan or position reversals. Four compact frame-0 screenshots cover the human-calibration subjects.

The accepted director canaries remain untouched. Seven regenerate byte-identically; `DIRECTOR-TERRAIN-mountain` intentionally regenerates differently under the authorized Matterhorn policy (`b03d581f…` accepted, `d0b7780a…` current) and was not repinned.
