# CHANGELOG — Resolve authority bundle v1.6 (correction of v1.5)

- **Parent:** v1.5.0, branch `docs/resolve-authority-freeze-v1.5` @ `9d944e01358b4b88d6ee4c9993a4db4084ba23ad`, manifest `a40c6954fbe7f72d48eaf3957c0ac036c471a7a92496e277660530386e7aba5a`. Grandparent v1.4.0 @ `d2d77680047221f2dac52adce6938dd410e38b47` (`34a7d0507a5d7f31769a46561d97219869e3525889f121424623eab4ff1dd84f`); v1.3.0 @ `1c9e090fcc8978e6e9b8f5dd538849448044d26b`; v1.2.0 @ `c6d1284c6f395f5b21a6d14f8c2873bccfc065bb`; v1.1.0 @ `47ddb225335b8c85ca5255b1de86ff508e0e8e91`; v1.0.0 @ `e2874f6f67f5adb3a5ced0e0e138c370e38995cb`.
- **This version:** 1.6.0, directory `docs/resolve-integration/v1.6/`, own `FREEZE-MANIFEST.json` (schema `resolveFreezeManifest.v1.6`; parent head + manifest pinned as schema constants and semantic checks).
- **Inputs of record:** Codex's final independent forensic adjudication of v1.5 (`~/outputs/resolve-v1.5-final-adjudication/REVIEW.md` + `findings.json`: 3 BLOCKER, 4 MAJOR — F15-01 … F15-07; gate verdicts MERGE infrastructure-only YES, PROBE_M0A permissible-not-authorized, REVIEW raw-first permissible, REFREEZE / ORDINARY_M0 / PRE_M3 BLOCKED); Claude's independent architectural adjudication (M-RAW M0-exit MAJOR; M-H0, M-PRECEDENCE, M-ID pre-M3 MAJORs; minors m-STATUS, m-TYPE, m-MCP); the qualified non-authoritative prototype (`~/outputs/resolve-v1.6-raw-evidence-prototype/`, 118/118 offline); the M0A driver compatibility findings (`M0A-PROBE-COMPATIBILITY.md`); Hermes's operational review PASS of v1.5.
- **Scope discipline:** no Resolve launched or mutated; no M0; no `EKA`; no adapter and no production capture driver; v1.0–v1.5 bytes untouched; unrelated Earth Studio work untouched. The v1.5 gate verdicts stand until this bundle is independently reviewed.

## New authorities (documents)

`RAW-CAPTURE.md` (facts-only capture format, forbidden fields, typed value codec, reusability law), `CAPTURE-SHIM.md` (what may produce a capture and what it may never do), `REFERENCE-PARSER.md` (the versioned derivation, its identity and the closed class vocabulary), `PRIMITIVE-SPEC.md` (the expectation authority, its digest and the hypothesis law), `REVIEW-REFREEZE.md` (review law, refreeze law, content-bound active matrix, derived promotion state), `IDENTITY-EVIDENCE.md` (claims A/B/C and what is never claimed), `EVIDENCE-ROOT.md` (append-only, content-addressed evidence layout), `M0-PHASES.md` (M0A/M0B/M0C/M0D and their entry evidence), `M0A-PROBE-COMPATIBILITY.md` (what a driver must change before running), `THREAT-MODEL.md` (defensive controls and residual risks).

## Changed authorities

- `tools/authority_lib.py` — **raw capability evidence**: `RAW_SCHEMA_ID`, `MECHANICAL_OUTCOMES`, `FORBIDDEN_RAW_FIELDS`, `DERIVED_CLASSES`/`DERIVED_FAMILY`, the typed-codec validators (`codec_errors`, `codec_broad_type`, `codec_contains_elision`), `raw_capture_digest`, `semantic_raw_capture`; **primitive spec authority** (`primitive_spec`, `spec_errors`, `spec_entry_digest`, `primitive_spec_digest`, `probe_spec_index`); **reference parser** (`derive_capability_result`, `parser_sha256`, `classification_family`, `derived_result_digest_ok`); **content-bound matrix** (`capability_matrix_digest`, `active_authority_errors`, `refreeze_block_errors`); **qualification** (`capability_qualification`, rewritten `primitive_status`, `callable_method_set` refusing a non-active matrix); **record laws** (`semantic_review_decision`, `semantic_refreeze_record`, `semantic_derived_result_record`, `semantic_identity_observation`); **identity claims** (`identity_claims`); **derived promotion state** (`promotion_state`, `promotion_step`, `PROMOTION_STATES`); **H0 gate** (`guard_record_errors`, `PLAN_VALIDATOR_ID`, `GUARD_CURRENT` and `PLAN_VALIDATED` rewritten in `evaluate_eligibility`, which now also refuses a non-active matrix up front); **journal** `CHECKPOINTED` mandatory with checkpoint evidence; **verification** compares `applied_operation_ids` and `unobserved_domains`; **protected surface** `PROTECTED_SURFACE_EXCLUSIONS` (and `semantic_snapshot` rejects observing them); **composed path** `schema_validate` is a required parameter of `validate_transaction_set` and `commit_eligibility`; `AUTHORITY_SURFACE` extended (`promotion_state` authorizing; every new helper INTERNAL_NON_AUTHORIZING); `RECORD_TYPES` gains `RAW_CAPABILITY_CAPTURE`, `DERIVED_CAPABILITY_RESULT`, `REVIEW_DECISION`, `IDENTITY_UNIQUENESS_OBSERVATION`, `IDENTITY_STABILITY_OBSERVATION` and `RETIRED_RECORD_TYPES` names the v1.5 capability-evidence type; `validate_evidence_set` runs the new record laws and returns sorted errors; `semantic_manifest` pins parent v1.5.
- `tools/capture_shim_reference.py` — **new** reference capture shim and append-only evidence root (TOOL; exercised against fake in-process objects only).
- `CAPABILITIES.json` (+schema v1.6) — `matrix_content_law`; refreeze block with parent matrix, parser and primitive-spec digests and four promotion lists; v1.6 evidence entries (raw/derived/review digests, receiver class, parser and spec digests, `result: null`) with **no** success or observed-result field; prior-version records marked `UNQUALIFIED_PRIOR_VERSION`; pipeline restated as M0A → derive → review → refreeze → active → derived promotion state.
- `READ-PRIMITIVES.json` (+schema v1.6) — full expectation spec per primitive (`receiver`, `arg_types`, `nullable`, `expected_type`, `shape_rule`, `completeness`, `expectation_status: DOCUMENTED_HYPOTHESIS`, `expectation_source`), `primitive_spec_sha256`, `primitive_spec_law` (`frozen_allowed: false`), probe `evidence_output: RAW_CAPABILITY_CAPTURE`, `capture_shim_contract`, `phases`, derived-class failure taxonomy and failure law.
- `schemas/resolveEvidenceSet.schema.json` v1.6 — capture body schema with the typed-value `$defs`, conditional outcome blocks and `additionalProperties: false` (so an interpretation field is schema-invalid); derived-result, review-decision, refreeze-record, identity-observation, guard-record and plan-validation record rules; retired type removed from the enum.
- `schemas/resolveSnapshot.schema.json` v1.6 — `method_provenance` cites `raw_capture_record_id`; no property/fade/speed/take payload.
- `schemas/resolveGuard.schema.json` — unchanged v3 digest law, with the guard object now carried inside the `GUARD_SNAPSHOT` record.
- Provisional schemas — verification requires `applied_operation_ids` and `unobserved_domains`; plan validation records carry `validator`, `h0_guard_digest`, `h0_snapshot_sha256`, `stages_completed`.
- `TARGET-CONTRACT.json`, `PERMISSIONS.json`, `MILESTONE-MATRIX.json`, `M3-PROBES.json`, eligibility request schema (`plan_target`) — version bumps and the additions named above.
- Documents rewritten or extended: `M0-PROBE-CONTRACT.md`, `ELIGIBILITY.md`, `GUARD-AUTHORITY.md`, `SEMANTIC-VALIDATION.md`, `SNAPSHOT-COMPLETENESS.md`, `SNAPSHOT-CONCURRENCY-RECOVERY.md` (S32/S33 scope banner and pointers), `AUTHORITY-PRECEDENCE.json/.md` (S32–S38, new documents, retired terms, journal-state enumeration authority), `MILESTONES.md`, `TARGET-ATTACHMENT-GATE.md`, `CANONICALIZATION.md` (new domains + the value codec), `IDENTITY-BINDING.md`, `PROVISIONAL.md`, `SCORECRAFT-EXTRACTION.md`, `README.md`.
- Tools: `tools/build_v1_6.py`, `tools/validate_v1_6.py` (new sections `raw-capture`, `codec`, `shim-safety`, `re-parser`, `raw-vs-fake-parse`, `review-law`, `refreeze-law`, `matrix-integrity`, `promotion`, `identity-evidence`, `h0-early-gate`, `protected-surface-exclusions`, `ghost-operation`, `v1.5-attack`, plus the precedence machine check), `tools/build_manifest.py` (parent v1.5), `tools/fixture_evidence.py` (captures minted through the reference shim against fake receivers; reviews, refreezes, guard records and identity observations).
- Fixtures regenerated under v1.6 rules (counts in `FINDING-RESOLUTION-MATRIX-v1.6.md` and `VALIDATION-REPORT.md`). Inherited unchanged: `fixtures/parse/`, `fixtures/timebase/`.

## Retired in v1.6

The v1.5 capability-evidence record type and its probe-authored `parse` / `parsed_observation` / `result` blocks; `capability_success` and `capability_record_qualifies`; `raw_evidence_sha256`, `review_raw_evidence_sha256`, `promoted_evidence_sha256`, `capability_evidence_record_id`; the optional `schema_validate=None` defaults; `APPLIED_n` as lifecycle prose. `tools/build_v1_5.py` and `tools/validate_v1_5.py` are replaced.

## Mutation prohibition preserved

No Resolve launched or mutated; no `EKA`; no M0 begun; no adapter or capture driver implementation; mutation schemas remain PROVISIONAL_UNTIL_M3; `mutation_allowed` only at M3 with `MIKKO_M3_AUTHORIZATION`, `TARGET_STATE_SCRATCH_WRITE_READY` and the H0/S0 early write gate; zero `QUALIFIED_READ` rows; `refreeze.reviewed: false`; every primitive expectation is a documented hypothesis.
