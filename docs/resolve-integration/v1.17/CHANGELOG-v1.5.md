# CHANGELOG — Resolve authority bundle v1.5 (narrowly scoped correction of v1.4)

- **Parent:** v1.4.0, branch `docs/resolve-authority-freeze-v1.4` @ `d2d77680047221f2dac52adce6938dd410e38b47`, manifest `34a7d0507a5d7f31769a46561d97219869e3525889f121424623eab4ff1dd84f`. Grandparent v1.3.0 @ `1c9e090fcc8978e6e9b8f5dd538849448044d26b` (`ad1e6bfcbcb41d79c230795d64e031438e8e39a194f640daca68532d798a55dc`); v1.2.0 @ `c6d1284c6f395f5b21a6d14f8c2873bccfc065bb`; v1.1.0 @ `47ddb225335b8c85ca5255b1de86ff508e0e8e91`; v1.0.0 @ `e2874f6f67f5adb3a5ced0e0e138c370e38995cb`.
- **This version:** 1.5.0, directory `docs/resolve-integration/v1.5/`, own `FREEZE-MANIFEST.json` (schema `resolveFreezeManifest.v1.5`; parent head + manifest pinned as schema constants and semantic checks).
- **Rationale:** Codex's forensic review of v1.4 (1 BLOCKER, 6 MAJOR, 0 MINOR; Hermes operational review PASS for the intended M0 operator flow), as enumerated in Mikko's v1.5 mission (26 items): (F1 MAJOR M0) a parsed capability failure could still qualify under a hypothetical reviewed matrix; (F2 MAJOR M1) duplicate occurrence ids could make verification order-dependent and produce false VERIFIED; (F3 MAJOR pre-M3) unrelated source-bound and timeline metadata changes escaped delta detection; (F4 BLOCKER pre-M3) direct commit validation could accept a false VERIFIED without resolving S0/S1; (F5 MAJOR pre-M3) journal readback/session not bound tightly to the supplied S1; (F6 MAJOR M1) composed validation could omit capability-to-snapshot enforcement; (F7 MAJOR pre-M3) a weaker S1 coverage profile could bypass write-readback completeness. Only those defects and their immediate invariants are corrected. The Codex review file was not present on disk; the mission's enumeration is the input of record.
- **Scope discipline:** no Resolve launched or mutated; no M0; no `EKA` access; no adapter; v1.0–v1.4 bytes untouched; unrelated Earth Studio work untouched.

## Changed authorities
- `tools/authority_lib.py`: **`AUTHORITY_SURFACE`** (authorizing: `commit_eligibility`, `validate_transaction_set`, `evaluate_eligibility`, `derive_attachment_state`, `primitive_status`, `callable_method_set`; everything else INTERNAL_NON_AUTHORIZING and so documented); **capability pipeline** `capability_success` (the one canonical predicate: raw exists + re-hashes + same session, parse succeeded, no parse error, no getter exception, receiver and `expected_type` match, shape ok, non-null, label SUCCESS **and** derived classification SUCCESS), `classify_capability_record` (derived from parse facts), `capability_record_qualifies(...) → (ok, reasons)` (+ review references this exact raw hash, promotion names this exact raw hash, active authority version + matrix sha), `primitive_status` additionally requires the active refreeze block to be reviewed and to list the exact probe id **and** raw evidence hash; `semantic_capability_record` rejects any label that contradicts the derived classification; **occurrence identity** `occurrence_index` (duplicate OBSERVED unique_id → `DUPLICATE_OCCURRENCE_IDENTITY`, canonical addresses, no index built), `derive_delta_errors`; **protected surface** (`PROTECTED_ITEM_FIELDS`, `PROTECTED_TIMELINE_FIELDS`, `PROTECTED_TRACK_FIELDS`, `UNPROTECTED_TIMELINE_FIELDS`) with `derive_delta` returning item added/removed/changed, timeline changes, track topology/field changes, marker and media-dependency changes; `verify_transaction` derives `unrelated` over the whole surface (APPEND may extend `end_frame` exactly to the new occurrence end); **journal** records carry `session_id`; `READBACK_S1` carries `readback_snapshot_sha256` + `readback_guard_digest`; `semantic_journal(records, plan, s1)` binds exactly one readback event to the supplied S1 and the execution session; `journal_readback`; **verification/commit** helpers require every input (plan, S0, S1, journal, active) and refuse partial/hash-only input; `semantic_verification_result` compares the declared result with the derived truth and with the journal readback digest, checks `session_id`; `semantic_commit_manifest` checks `session_id`, `s1_snapshot_sha256`; **snapshot** `semantic_snapshot(snap, ctx)` derives the callable set itself and enforces `provenance_errors` (every OBSERVED field/domain method cited in `collection.method_provenance` as `QUALIFIED_OBSERVATION` backed by a qualifying CAPABILITY_EVIDENCE record under the active matrix; CANDIDATE_OBSERVATION never backs OBSERVED); **profiles** `APPEND_VERIFY`, WRITE_PRECHECK now mandates source bounds, `profile_satisfies`, `required_s1_profile`, `OPERATION_VERIFY_PROFILE`, `S0_REQUIRED_PROFILE`; **composed path** `validate_transaction_set` (explicit `VALIDATION_STAGES`, no callable-set/skip parameter) and `commit_eligibility` (the only authorizing commit entry point; returns eligible/errors/stages); guard **v3** (`provenance_sha256`); `semantic_manifest` pins parent v1.4.
- `CAPABILITIES.json` (+schema v1.5): `refreeze.promoted_evidence_sha256`; `qualification_pipeline` restated as RAW_EVIDENCE → PARSE → PARSED_RESULT → REVIEW → REFREEZE → REFREEZE_RECORD → ACTIVE_CAPABILITY; `semantic_capabilities` requires QUALIFIED_READ evidence to be explicitly promoted (probe id + raw hash).
- `READ-PRIMITIVES.json` (+schema v1.5): `expected_type` per primitive (documented 21.1 return type; provisional expectation) + `expected_type_note`.
- `schemas/resolveEvidenceSet.schema.json` v1.5: `CAPABILITY_EVIDENCE.parse` block, `parsed_observation` typed, `qualification.review_raw_evidence_sha256`, `promoted_by.raw_evidence_sha256`.
- `schemas/resolveSnapshot.schema.json` v1.5: `collection.method_provenance` (replaces the v1.4 collection status map), `APPEND_VERIFY` profile, payload domain v1.5, guard v3; `schemas/resolveGuard.schema.json` v3 (`provenance_sha256`).
- Provisional schemas: plan `session_id`; journal `session_id`, `readback_snapshot_sha256`, `readback_guard_digest`; verification `session_id`; commit `session_id`, `s1_snapshot_sha256`.
- `TARGET-CONTRACT.json`, `PERMISSIONS.json`, `MILESTONE-MATRIX.json`, `M3-PROBES.json`, eligibility request schema: version bumps only (laws unchanged).
- Documents: new `CHANGELOG-v1.5.md`, `FINDING-RESOLUTION-MATRIX-v1.5.md`; rewritten `SEMANTIC-VALIDATION.md`, `SNAPSHOT-COMPLETENESS.md`, `M0-PROBE-CONTRACT.md`, `GUARD-AUTHORITY.md`, `CANONICALIZATION.md`, `README.md`; edited `ELIGIBILITY.md`, `TARGET-ATTACHMENT-GATE.md`, `PROVISIONAL.md`, `MILESTONES.md`, `SCORECRAFT-EXTRACTION.md`, `AUTHORITY-PRECEDENCE.json/.md` (S25–S31; v1.4 changelog/matrix → HISTORICAL; retired terms).
- Tools: `tools/build_v1_5.py`, `tools/validate_v1_5.py` (new sections: capability pipeline, occurrence-uniqueness, delta-surface, unrelated-change-law, journal-s1-binding, s1-profile-law, bypass-audit, commit-eligibility, v1.4-attack, probe-vs-qualified; per-finding coverage table in the report), `tools/build_manifest.py` (parent v1.4), `tools/fixture_evidence.py` (parse blocks, review/promotion raw-hash bindings, seven new adversarial capability sets).
- Fixtures regenerated under v1.5 rules (counts in `FINDING-RESOLUTION-MATRIX-v1.5.md` and `VALIDATION-REPORT.md`). Inherited unchanged: `fixtures/parse/`, `fixtures/timebase/`.

## Inherited unchanged
`AUTHORIZATION-2026-09-08.md`, `DOCTRINE.md`, `TRANSPORT.md`, `DRIFT-POLICY.md`, `REVIEW-MARKER-POLICY.md`, `IDENTITY-BINDING.md`, `SNAPSHOT-CONCURRENCY-RECOVERY.md`, `CANARIES.md`, `CLIENT-VERSION-INVENTORY.md`, `M3-MATRIX.md`, `TIMEBASE.json` (+schema), `CANARY-SOURCE-MANIFEST.json` (+schema), `schemas/resolveTrackPolicy.*`, `QUARANTINE-MANIFEST.json`, provisional `resolveSequenceLineage`/`resolveBindingSet`/`resolveBindingObservation`/`resolveCheckpoint`, historical inputs.

## Removed relative to v1.4
`tools/build_v1_4.py`, `tools/validate_v1_4.py` (replaced); v1.4 generated fixtures (regenerated); `VALIDATION-REPORT.md` regenerated. No historical/direct validator from v1.4 remains as an authorizing surface: `semantic_commit_manifest`, `semantic_verification_result`, `semantic_journal`, `verify_transaction`, `derive_delta` remain only as INTERNAL_NON_AUTHORIZING helpers composed by `validate_transaction_set`, and they refuse partial input.

## Mutation prohibition preserved
No Resolve launched or mutated; no `EKA`; no M0 begun; no adapter implementation; mutation schemas remain PROVISIONAL_UNTIL_M3; `mutation_allowed` only at M3 with `MIKKO_M3_AUTHORIZATION` and `TARGET_STATE_SCRATCH_WRITE_READY`; zero `QUALIFIED_READ` rows; `refreeze.reviewed: false`; the APPEND effect law and `APPEND_VERIFY` profile are provisional and claim no runtime semantics.
