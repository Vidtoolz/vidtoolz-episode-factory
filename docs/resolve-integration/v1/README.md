# Resolve execution subsystem: authority bundle v1

Frozen 2026-09-08 under Mikko's authorization (`AUTHORIZATION-2026-09-08.md`). Read `FREEZE-MANIFEST.json` first: it lists every file with its sha256 and status (`FROZEN_NOW`, `PROVISIONAL_UNTIL_M3`, `UNTESTED_BLOCKED`). A file whose hash differs from the manifest is not the frozen version. `PROVISIONAL.md` says what is deliberately not frozen. Nothing in this bundle is human approval of any run, gate or publication. Registered in `docs/DOC-AUTHORITY.md`. Quarantined prototypes live in `experiments/quarantine-2026-09-08/`.
