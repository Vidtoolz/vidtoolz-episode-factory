# Mikko authorization — Resolve execution subsystem authority-freeze phase (2026-09-08)

Recorded from Mikko's written authorization of 2026-09-08 (chat). Substance, not verbatim in full.

- **Phase approved:** authority freeze as described in `ADJUDICATION-FREEZE-CONTRACT.md`. Does NOT permit mutation-capable Resolve implementation.
- **A1 doctrine APPROVED:** Resolve automation only as a governed execution capability beneath Episode Factory authority. Permitted deterministic scope: inspection; Directed Draft materialization; controlled revision; mechanical QC; deterministic repair where separately qualified; delivery/render where separately qualified. Episode Factory remains canonical production truth. Resolve is not an independent production authority. MCP is not an authority. Mikko remains final creative authority. Manual human edits must never be silently overwritten. The previous `edit_mode: MANUAL` doctrine may be superseded only for workflows explicitly opted into the frozen Resolve execution subsystem; unrelated existing workflows are not reinterpreted as automatically editable.
- **A4 host/library APPROVED:** adjudicated host (vidnux); isolated Local Database; disposable scratch projects/timelines; no shared production project state. ROJEKTI/EKA MUST NOT be used for qualification. Do not describe opening EKA from 21.1 as automatically upgrading the library (unsupported claim); the prohibition exists because the library holds shared production state and project-level cross-version consequences are unqualified. Before later shared-library qualification, inventory Resolve versions on PRESTO, vidnux, VIDLAP2 and document compatibility implications.
- **Quarantine AUTHORIZED:** as specified in A2; preserve contents; do not delete; move/reclassify into the experimental namespace; rename colliding schema identities; prove canonical production code no longer resolves or emits the experimental authority.
- **Freeze bundle AUTHORIZED:** must include or fingerprint the adjudication contract, doctrine amendment, quarantine disposition, transport, host/library, lineage, identity/binding, timebase + 21-boundary fixtures, snapshot/concurrency/recovery doctrine, capability matrix, canary references and hashes (MP4, 22 media, review/lock), pre-M2 schemas, and an explicit list of what remains provisional until M3. Distinguish FROZEN_NOW / PROVISIONAL_UNTIL_M3 / UNTESTED_BLOCKED.
- **Write prohibition** until the before-M2 bundle is complete, hashed, independently verified and reported: no Resolve mutation; no mutation-capable production implementation; no shared-library use; no production project modification; no production code change except the authorized quarantine/freeze authority work; no remote production-state modification; no implementation around an unresolved capability row.
- **Next authorized work (Hermes may coordinate):** doctrine authority update; quarantine; host/library authority record; freeze-bundle construction; Resolve client-version inventory; before-M2 hash verification; preparation of M0 scratch qualification; read-only schema/code planning. No mutation-capable milestone begins.
