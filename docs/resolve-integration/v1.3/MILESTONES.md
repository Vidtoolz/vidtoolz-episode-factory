# MILESTONES.md — exact M0/M1/M2/M3 boundaries (v1.3, FROZEN_NOW)

Permission declarations: `PERMISSIONS.json` (default DENY). Eligibility: `ELIGIBILITY.md`. Machine cross-check: `MILESTONE-MATRIX.json`. `SCHEMA-VALID != AUTHORIZED TO MUTATE`. No milestone below is begun by this bundle.

| M | Entry gate | Work allowed | Exit evidence | Prohibited |
|---|---|---|---|---|
| **M0 — read-only connection and observation** | bundle independently hash-verified (`BUNDLE_VERIFICATION` record, verifier ≠ preparer); derived target state `ATTACHMENT_READY` (`PROVISIONING_RECORD`, pinned `LAUNCH_RECIPE`; `TARGET-ATTACHMENT-GATE.md`); every operation carries its `target_requirement` (SESSION / PROJECT / PROJECT_TIMELINE, `ELIGIBILITY.md`); PRESTO/VIDLAP2 versions recorded or explicitly deferred | 1) `READ_PRIMITIVE_QUALIFICATION_PROBE` (first and, at v1.3, the only session-establishing operation; SESSION scope; calls each `probe_candidate` getter per `M0-PROBE-CONTRACT.md` and records CANDIDATE `CAPABILITY_EVIDENCE` + `CONNECTION_OBSERVATION`; promotes nothing); 2) once the connection observation derives `ATTACHED_READ_ONLY`: `ENUMERATE_PROJECTS` (SESSION), `ENUMERATE_TIMELINES` (PROJECT binding), `SNAPSHOT_CAPTURE` (PROJECT_TIMELINE binding; `MINIMAL_M0`, honest `complete:false` with `incomplete_reasons`, partial items kept with status, failures in the ledger), `TRIPWIRE_READ` (PROJECT, idle only); `CONNECT` and `TIMEBASE_OBSERVE` have REFUSE fallbacks and are **not eligible** until a reviewed refreeze qualifies their getters; unqualified primitives are never called silently | `M0-EVIDENCE/`: connection, launch-recipe sha, per-method evidence records, ≥2 consecutive snapshots with equal guard digests per target, timebase observation, read-only journal, report → bundle v1.4 reviewed refreeze of read rows (`REFREEZE_RECORD` kind `M0_READ_REQUALIFICATION`) | any mutator incl. `SaveProject`, `CreateEmptyTimeline`, `ImportMedia`, markers, settings; `EKA`; user's Local Database; Mikko's live session |
| **M1 — executable contracts (offline)** | M0 exit evidence | Node canonicalization reproducing all vectors and rejections; executable validators; timebase implementation reproducing the 21-boundary fixture; binding-set generation from the canary (dry-run); occurrence identity resolver over M0 snapshots; read-only mechanical QC; journal/recovery state machine against fixtures | vectors/fixtures pass in Node and Python; resolver behaves on ≥3 synthetic snapshots; recovery reaches a terminal state for every injected fault | Resolve calls beyond re-reading M0 targets (read ops require `M0_EXIT_EVIDENCE`) |
| **M2 — read-only attach and dry-run** | M1 exit | attach collector (`ATTACHED_READ_ONLY`); Canary C1; `SetCurrentTimeline` on adapter-prefixed projects only; `BUILD_MUTATION_PLAN_DRY_RUN` against a real guard; comparator on an operator-made change; pre-write revalidation detecting the change with zero mutators; PREPARED records never executed | C1 pass; comparator/revalidation detect the human change; dry-run plan passes schema + semantic layers | any mutator; `SaveProject`; canary media |
| **M3 — scratch qualification (first mutation-capable)** | M2 exit; Mikko's explicit M3 authorization token; target `SCRATCH_WRITE_READY`; PREPARED journal before every mutator | the 19 probes P1–P19 of `M3-MATRIX.md`/`M3-PROBES.json` on disposable `VIDTOOLZ_RESOLVE_QUAL_V1_*` projects with synthetic media; **scratch lifecycle** narrowly permitted: `CreateProject`, `CreateEmptyTimeline`, `SaveProject`, `LoadProject_adapterPrefixed` | per-probe evidence; bundle v2.0.0 refreeze per the refreeze law | canary media; `EKA`; user's Local Database; non-prefixed project; `SetCurrentDatabase`; ripple delete; `ReplaceClip`; render; any M4+ operation |

Later milestones remain as described in the historical adjudication; none is authorized here. `MUTATION_AUTHORIZED` is unreachable before bundle v2.0.0 plus Mikko's activation.
