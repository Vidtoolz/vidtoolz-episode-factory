# M0-PROBE-CONTRACT.md — READ_PRIMITIVE_QUALIFICATION_PROBE (v1.3, FROZEN_NOW as contract; nothing here has run)

Machine form: `READ-PRIMITIVES.json.logical_operations.READ_PRIMITIVE_QUALIFICATION_PROBE` (`purpose_is_qualification: true`, `read_only: true`, `promotes_capability: false`, `evidence_output: CANDIDATE_EVIDENCE`, every primitive `probe_allowed: true`), `PERMISSIONS.json` entry `M0 / READ_PRIMITIVE_QUALIFICATION_PROBE` (prerequisite `PROBE_ALLOWED_PRIMITIVES`), `CAPABILITIES.json` rows with `probe_candidate: true`.

**PROBE_ALLOWED != QUALIFIED_READ.** A probe may call a `DOCUMENTED_NOT_QUALIFIED` getter because calling it *is the test*. Its result is recorded, never acted on. `evaluate_eligibility` expands the probe with `PROBE_ALLOWED_PRIMITIVES`: a primitive is callable in the probe iff its capability row is `probe_candidate: true` and the read-primitive entry says `probe_allowed: true`. Every other logical operation uses `PRIMITIVES_QUALIFIED_OR_DECLARED_UNOBSERVED`: a primitive is callable iff its row is `QUALIFIED_READ` **and** a `CAPABILITY_EVIDENCE` record for the same method, host, version and build with a non-null `reviewed_refreeze_version` is linked in the evidence set; otherwise the declared fallback applies (`MARK_UNAVAILABLE`, `DOWNGRADE_COVERAGE`) or the operation is refused (`REFUSE`).

| Question | Answer |
|---|---|
| Which getters may it test | exactly the 47 methods listed under the probe in `READ-PRIMITIVES.json` (all `read:` rows of `CAPABILITIES.json`, each `probe_candidate: true`); nothing else, no mutator, no `run_script` |
| Why testing them is allowed | they are documented in the 21.1 stub/README but have no evidence on 21.1.0 build 14; the probe's purpose is qualification (`purpose_is_qualification: true`); results are never used to build a snapshot, plan or decision |
| Required target scope | `SESSION` (host/library/session identity). Derived attachment state must be at least `ATTACHMENT_READY` (valid `PROVISIONING_RECORD`, `LAUNCH_RECIPE` pinned to the contract binary and version with `Local` scripting preference, independent `BUNDLE_VERIFICATION` for 1.3.0); `READ_ONLY_JOURNAL` open; library not prohibited. No project or timeline binding is required or established |
| Session | isolated qualification session per `TARGET-CONTRACT.json.session` (dedicated config/support roots, port 1144 closed, never Mikko's live session, never `EKA`, never `Local Database`) |
| How evidence is captured | one `CAPABILITY_EVIDENCE` record per method: `method`, `host`, `resolve_version` (`21.1.0.0014`), `build` (14), `observed_result` (type and shape, values hashed not copied), `evidence_sha256` (hash of the raw capture file), `reviewed_refreeze_version: null`; plus one `CONNECTION_OBSERVATION` (db_type, db_name, product, version string, root); all records content-addressed (`record_id = sha256(domain + canonical body)`) and appended to the read-only journal |
| How failure is recorded | a method that raises, times out or returns an unexpected type produces a `CAPABILITY_EVIDENCE` record with `observed_result` beginning `FAILED:` and the exception class; the probe continues with the next method; the run is `INCOMPLETE` if any method produced no record; nothing is retried silently |
| What the result is | **CANDIDATE EVIDENCE**, not qualification. `promotes_capability: false` is machine-checked. `CAPABILITIES.json` is not modified by the run |
| How qualification happens | a human-reviewed refreeze (bundle v1.4) sets `reviewed_refreeze_version` on the accepted records, moves the corresponding rows to `QUALIFIED_READ` with `evidence_records` carrying `version_match: true`, and records a `REFREEZE_RECORD` (`kind: M0_READ_REQUALIFICATION`, `reviewed: true`). Only then does `PRIMITIVES_QUALIFIED_OR_DECLARED_UNOBSERVED` pass for `CONNECT`/`TIMEBASE_OBSERVE` |
| Ordinary M0 read path before qualification | `SNAPSHOT_CAPTURE` is eligible in degraded mode (all its primitives declare `MARK_UNAVAILABLE`/`DOWNGRADE_COVERAGE`): fields get explicit status, coverage `complete: false` with `incomplete_reasons`; `CONNECT` and `TIMEBASE_OBSERVE` (`REFUSE` fallbacks) are **not eligible** until a reviewed refreeze — the probe is what establishes the session boundary at v1.3 |

Fixtures: `fixtures/eligibility/cases.json` (`m0-probe-allow-attachment-ready`; denials for unprovisioned, bad UUID, wrong binary pin, self-verified bundle, bundle verified for another version, missing journal, declared state, EKA observation, tampered record; `m0-connect-deny-*` incl. unreviewed candidate evidence and reviewed evidence against the frozen matrix; `m0-connect-allow-hypothetical-refrozen-matrix` against `fixtures/eligibility/capabilities-hypothetical-refreeze.json`, which is explicitly **HYPOTHETICAL_NOT_AUTHORITY**).

This contract authorizes no run. M0 begins only on Mikko's instruction after independent review of this bundle.
