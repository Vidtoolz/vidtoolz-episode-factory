# FINDING-RESOLUTION-MATRIX-v1.2.md

| # | Finding (Codex / mission item) | Severity | Correction | Where | Status |
|---|---|---|---|---|---|
| C1 | permission lookup ≠ authorization eligibility (§1) | MAJOR | declaration vs eligibility split; closed prerequisite codes per entry; deterministic `evaluate_eligibility` returning permitted_by_policy / prerequisites_satisfied / eligible / failed_prerequisites / reason_codes; unknown input fails closed; 20 eligibility fixtures | `ELIGIBILITY.md`, `PERMISSIONS.json`, `authority_lib.py`, `fixtures/eligibility/` | RESOLVED |
| C2 | target attachment gate (§2) | MAJOR | states UNPROVISIONED → PROVISIONED_NOT_VERIFIED → ATTACHMENT_READY → ATTACHED_READ_ONLY → SCRATCH_WRITE_READY with required evidence; semantic check; UUID/root never fabricated; M0 read attach needs no mutation prerequisite | `TARGET-ATTACHMENT-GATE.md`, `TARGET-CONTRACT.json`, schema | RESOLVED |
| C3 | read-primitive mapping (§3) | MAJOR | `READ-PRIMITIVES.json` logical op → primitives with evidence class/ref/milestones/fallback; `PRIMITIVES_QUALIFIED_OR_DECLARED_UNOBSERVED`; `READ_PRIMITIVE_QUALIFICATION_PROBE` is the only op allowed to call unqualified getters | `READ-PRIMITIVES.json`, `ELIGIBILITY.md` | RESOLVED |
| C4 | snapshot false completeness (§4) | MAJOR | coverage profiles MINIMAL_M0 / FULL_TIMELINE_READ / WRITE_PRECHECK; closed domain vocabulary; per-field `field_status` with reasons; nullable ids; `observation_ordinal`; 15 negative snapshot fixtures | `SNAPSHOT-COMPLETENESS.md`, snapshot schema, `semantic_snapshot` | RESOLVED |
| C5 | item range consistency (§5) | MAJOR | end ≥ start; source bounds; timeline bounds; `duration_convention` UNQUALIFIED with two-candidate check, exact under qualified conventions | snapshot schema/semantics | RESOLVED |
| C6 | marker sort key non-total (§6) | MAJOR | total key (address, frame, duration, custom_data, name, color, note); (address, frame) collision and exact duplicates rejected; vectors: reversed input same digest, note-only and duration-only differences, Unicode byte-distinct | `CANONICALIZATION.md`, `sort_markers`, vectors + rejections | RESOLVED |
| C7 | `$f64` tag unvalidated (§7) | MAJOR | 16 lowercase hex; NaN/Inf and negative zero rejected; `$rational` pattern + reduced; rejection fixtures | `authority_lib.validate_f64/validate_rational` | RESOLVED |
| C8 | duplicate JSON keys (§8) | MAJOR | policy A: strict parser (`strict_loads`) at the parse boundary; all authority JSON loaded strictly; fixture | `CANONICALIZATION.md §5`, `fixtures/parse/` | RESOLVED |
| C9 | QUALIFIED_READ evidence granularity (§9) | MAJOR | evidence records (host, version, build, run_ref, method, observed_result, path, sha, version_match); every read row downgraded (no 21.1 b14 evidence exists); readability vs survival split | `CAPABILITIES.json`, `semantic_capabilities` | RESOLVED |
| C10 | authority precedence (§10) | MAJOR | `AUTHORITY-PRECEDENCE.json/.md`: per-document status, 10 superseded statements (old M0 --dump-tools restriction, payload guard, snapshot wording, milestone/refreeze wording, marker shorthand, IMPORT_OVERRIDE, prose prerequisites, QUALIFIED_READ rows, marker key, Scorecraft guards); retired-term scan | `AUTHORITY-PRECEDENCE.*` | RESOLVED |
| C11 | milestone consistency (§11) | MAJOR | `MILESTONE-MATRIX.json` derived from PERMISSIONS; validator asserts M0/M1/M2 no mutation, M3 only mutation milestone, EKA never a scope, every M3 probe op permitted | `MILESTONE-MATRIX.json`, validator | RESOLVED |
| C12 | semantic validators incomplete (§12) | MAJOR | plan/journal/commit/recovery rules per SEMANTIC-VALIDATION §v1.2; transition table includes RECOVERY_RECONCILING and all documented terminals | `authority_lib.py`, fixtures | RESOLVED (still provisional) |
| C13 | schema + semantic layering (§13) | MAJOR | every fixture declares `layer_expected_failure`; positives must pass schema first; former schema-invalid commit positives replaced by full documents | `fixtures/layered/`, validator | RESOLVED |
| C14 | M3 plan inconsistencies (§14) | MAJOR | P0 = M0 preflight; 19 probes P1–P19 incl. import probe P19; operation names = PERMISSIONS names; recovery states aligned; lifecycle mapping P11/P15/P16 | `M3-MATRIX.md`, `M3-PROBES.json` | RESOLVED |
| C15 | Scorecraft guard statement (§15) | MINOR | guards recorded in P7 driver, production fixture and hardlink fixture; production driver still lacks the canonical guard | `SCORECRAFT-EXTRACTION.md` | RESOLVED |
| C16 | manifest validation (§16) | MINOR | `schemas/resolveFreezeManifest.schema.json` + `semantic_manifest`; suite loads the real manifest and 7 mutated negatives | validator | RESOLVED |
| C17 | M0 attachment eligibility fixtures (§17) | MAJOR | ALLOW/DENY set incl. unprovisioned, EKA, unknown library, production project, wrong host, missing evidence, version mismatch, no journal | `fixtures/eligibility/cases.json` | RESOLVED |
