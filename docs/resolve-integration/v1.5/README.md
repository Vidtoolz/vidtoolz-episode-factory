# Resolve execution subsystem: authority bundle v1.5

Versioned, narrowly scoped correction of v1.4 (parent branch `docs/resolve-authority-freeze-v1.4` @ `d2d77680047221f2dac52adce6938dd410e38b47`, parent manifest `34a7d0507a5d7f31769a46561d97219869e3525889f121424623eab4ff1dd84f`; grandparent v1.3 `1c9e090` / `ad1e6bfc…55dc`; v1.2 `c6d1284` / `69e1caec…b5e6`; v1.1 `47ddb22` / `83d8a307…48c5`; v1.0 `e2874f6` / `d9cd5478…8821`). `../v1/` … `../v1.4/` are immutable history. Read `FREEZE-MANIFEST.json` first (closed status vocabulary per file; sha256 and byte count per file), then `AUTHORITY-PRECEDENCE.md`, `CHANGELOG-v1.5.md` and `FINDING-RESOLUTION-MATRIX-v1.5.md`. `PROVISIONAL.md` lists what is not frozen.

Laws in one line each: `SCHEMA-VALID != AUTHORIZED TO MUTATE` (`SEMANTIC-VALIDATION.md`); `PERMISSION DECLARATION != ELIGIBILITY` (`ELIGIBILITY.md`); `PROBE_ALLOWED != QUALIFIED_READ`, `CANDIDATE_CAPABILITY_EVIDENCE != QUALIFIED_READ` and **capability success is derived from raw evidence + parse, never from a label** (`M0-PROBE-CONTRACT.md`); attachment state is derived from a coherent, current, envelope-bound evidence set against the active reviewed manifest (`TARGET-ATTACHMENT-GATE.md`); every OBSERVED snapshot field is backed by cited, qualifying capability evidence, `CANDIDATE_OBSERVATION != QUALIFIED_OBSERVATION`, and occurrence identity is unique within the timeline (`SNAPSHOT-COMPLETENESS.md`); verification is derived from S0 → S1 over the **protected surface**, the journal's readback event names the exact S1, and **`commit_eligibility` → `validate_transaction_set` is the only authorizing commit path** (`SEMANTIC-VALIDATION.md`).

Validate with `python3 -B tools/validate_v1_5.py` (report: `VALIDATION-REPORT.md`, with per-finding coverage; layers raw parse → schema → evidence-binding → attachment → capability → snapshot → semantic → eligibility → linked-set/composed authorization; seeded random-order property tests; bypass audit of the authority surface; exact-manifest binding against the real `FREEZE-MANIFEST.json`). Regenerate machine artifacts with `python3 -B tools/build_v1_5.py` from v1.4-seeded inputs, then the manifest with `python3 -B tools/build_manifest.py` after a passing validation, then validate again (two cycles converge; the report never embeds the manifest sha). Run every tool with `python3 -B`; a `__pycache__` directory is a validation failure.

Nothing in this bundle is human approval of any run, gate or publication; no Resolve was launched or mutated; `EKA` was not accessed; M0 has not begun; no adapter exists; mutation-capable schemas remain PROVISIONAL_UNTIL_M3; the capability matrix still has zero `QUALIFIED_READ` rows.
