# FINDING-RESOLUTION-MATRIX-v1.5.md

Codex forensic review of v1.4 (1 BLOCKER, 6 MAJOR, 0 MINOR; Hermes operational review PASS for the intended M0 operator flow) and Mikko's v1.5 mission items 1–26. Coverage per finding is reported in `VALIDATION-REPORT.md` (per-finding table); the total check count is not proof.

## Review findings

| # | Finding | Severity | Correction | Where | Status |
|---|---|---|---|---|---|
| F1 | parsed capability failure can still qualify under a hypothetical reviewed capability matrix | MAJOR (M0) | one canonical predicate `capability_success`: raw evidence exists, re-hashes, same session; parse succeeded, no parse error, no getter exception; receiver and `expected_type` match; shape ok, non-null; label SUCCESS **and** derived classification SUCCESS. `capability_record_qualifies` adds exact host/product/version/build, reviewed ACCEPT whose `review_raw_evidence_sha256` is this record's raw hash, `promoted_by` naming the active authority version, active matrix sha **and** this raw hash; `primitive_status` requires the active refreeze block (reviewed, `promoted_probe_ids`, `promoted_evidence_sha256`) to name the exact evidence. Labels/reviews/refreezes can never override a parsed failure (`semantic_capability_record`). | `authority_lib.capability_success/classify_capability_record/capability_record_qualifies/primitive_status`, `CAPABILITIES.json#refreeze/qualification_pipeline`, `READ-PRIMITIVES.json#expected_type`, `schemas/resolveEvidenceSet.schema.json` (parse block, review/promotion raw hashes), `M0-PROBE-CONTRACT.md`; fixtures `attached-evidence-parse-failed-accepted`, `-getter-exception-accepted`, `-label-success-parse-failed`, `-wrong-observed-type`, `-review-other-raw`, `-promoted-other-raw`, `-null-result`, `-no-raw`, `-raw-tampered`; eligibility `m0-connect-deny-hyp-*`; validator `capability`, `capability-record-negative`, `capability-qualification-negative` | RESOLVED |
| F2 | duplicate occurrence ids make verification order-dependent / false VERIFIED | MAJOR (M1) | `occurrence_index`: identity domain = timeline payload; duplicate OBSERVED `unique_id` → `DUPLICATE_OCCURRENCE_IDENTITY` (addresses in canonical order), no index built; `semantic_snapshot` flags duplicates in every profile; `derive_delta` returns None and `verify_transaction` → `UNOBSERVABLE_STATE` (never first/last pick); permutation tests prove identical rejection text and identical delta/verdict for unique inputs. Fail-closed offline; no composite tuple is claimed until M0/M3 observe real id behaviour. | `authority_lib.occurrence_index/derive_delta/verify_transaction`, `SNAPSHOT-COMPLETENESS.md §Occurrence identity`; fixtures `snapshot-duplicate-occurrence-identity*`, `verification-duplicate-identity-*`, `linked-set-duplicate-occurrence-identity*`; validator `occurrence-uniqueness` | RESOLVED |
| F3 | unrelated source-bound and timeline metadata changes escape delta detection | MAJOR (pre-M3) | protected surface defined: occurrence identity, track, start/end/duration, enabled, media pool id, media id, source start/end, source sha/locator/status, name, provenance kind, item markers; timeline identity, name, start/end frame, start timecode, fps, width, height, settings; track topology + name/enabled/locked; timeline markers; media dependencies (`is_current`, `duration_convention` explicitly unprotected). `derive_delta` covers it all; `verify_transaction` derives `unrelated` from it; a caller-declared `unrelated: []` is compared against the derived truth. | `PROTECTED_*` constants, `derive_delta`, `verify_transaction`, `SEMANTIC-VALIDATION.md §Protected surface`; fixtures `verification-derived-unrelated-*`, `verification-hides-unrelated-*`, `linked-set-*-unrelated-change`; validator `delta-surface`, `unrelated-change-law` | RESOLVED (provisional surface) |
| F4 | direct commit validation accepts false VERIFIED without resolving S0/S1 | **BLOCKER** (pre-M3) | option B: `commit_eligibility(ts, …)` is the ONLY authorizing commit entry point and always runs the full `validate_transaction_set` sequence (evidence authority → capability provenance → schema → S0 → plan → journal → S1 → delta → effects → verification → conflicts → commit); `semantic_commit_manifest` and `semantic_verification_result` are INTERNAL_NON_AUTHORIZING, require plan, S0, S1, journal, active and refuse hash-only/partial input; missing linked S0/S1 → INELIGIBLE; false VERIFIED → INELIGIBLE. `AUTHORITY_SURFACE` classifies every exported validator; the bypass audit checks names, docstrings, signatures and behaviour. | `authority_lib.commit_eligibility/validate_transaction_set/AUTHORITY_SURFACE`, `SEMANTIC-VALIDATION.md §Single authority`; fixtures `commit-without-s0-object`, `commit-without-s1-object`, `commit-direct-false-verified-consistent-hashes`, `linked-set-direct-false-verified-consistent-hashes`, `linked-set-commit-without-s1`; validator `bypass-audit`, `commit-eligibility` | RESOLVED |
| F5 | journal readback references/session not bound tightly to supplied S1 | MAJOR (pre-M3) | every journal record carries `session_id` (== plan execution session); exactly one `READBACK_S1` record, carrying `readback_snapshot_sha256` (= snapshot object digest of S1) and `readback_guard_digest`; `semantic_journal(records, plan, s1)` rejects a different S1, a missing readback where S1/verification exist, multiple readbacks, and an S1 from another session; verification's readback digest must equal the journal's; commit carries `session_id` and `s1_snapshot_sha256`. | `semantic_journal`, `journal_readback`, `semantic_verification_result`, `semantic_commit_manifest`, journal/plan/verification/commit schemas; fixtures `journal-readback-*`, `journal-session-*`, `journal-s1-from-other-session`, `verification-journal-readback-names-other-s1`, `verification-without-readback-event`, `verification-s1-from-other-session`, `linked-set-journal-names-other-s1`, `linked-set-s1-from-other-session`, `linked-set-missing-readback-event-with-verification`; validator `journal-s1-binding` | RESOLVED |
| F6 | composed validation can omit capability-to-snapshot enforcement | MAJOR (M1) | field provenance model `collection.method_provenance` (method → observation_class, capability_evidence_record_id, capability_matrix_sha256); `semantic_snapshot(snap, ctx)` derives the callable set from the active authority + evidence set and requires every OBSERVED field/domain method to be a `QUALIFIED_OBSERVATION` citing a qualifying record under the active matrix; `CANDIDATE_OBSERVATION` never backs OBSERVED; guard v3 binds `provenance_sha256`; `validate_transaction_set` builds the context itself (no caller-supplied callable set). | `provenance_errors`, `semantic_snapshot`, `guard_object`, `SNAPSHOT-COMPLETENESS.md §Field provenance`, `GUARD-AUTHORITY.md`; fixtures `snapshot-observed-without-provenance-entry`, `snapshot-observed-backed-by-candidate-observation`, `snapshot-provenance-*`, `snapshot-candidate-observation-honest-unavailable`, `linked-set-s0-observed-fields-without-provenance`; validator `probe-vs-qualified`, `bypass-audit` | RESOLVED |
| F7 | weaker S1 coverage profile bypasses write-readback completeness | MAJOR (pre-M3) | `OPERATION_VERIFY_PROFILE` (APPEND → `APPEND_VERIFY`; other ops none), `S0_REQUIRED_PROFILE = WRITE_PRECHECK`, `profile_satisfies` (superset law over mandatory domains/item fields/timeline fields/identity/locks/guard), `required_s1_profile`; S1 must satisfy the required profile and be complete; WRITE_PRECHECK now mandates source bounds so S0 can prove the protected surface. | `COVERAGE_PROFILES`, `validate_transaction_set` stage s1, `SNAPSHOT-COMPLETENESS.md §Coverage profiles`; fixtures `linked-set-s1-weaker-profile`, `linked-set-s1-incomplete`, `linked-set-s1-minimal-m0-profile`, positive `linked-set-committed-append-verify-profile`; validator `s1-profile-law` | RESOLVED (provisional profile) |

## Mission items

| # | Item | Status |
|---|---|---|
| 1–2 | capability success derived, not label-trusted; pipeline RAW_EVIDENCE → PARSE → PARSED_RESULT → REVIEW → REFREEZE → ACTIVE_CAPABILITY with exact upstream digests; seven adversarial fixtures | RESOLVED (F1) |
| 3–4 | occurrence id uniqueness law; order-independent occurrence indexing; permutation tests with and without duplicates | RESOLVED (F2) |
| 5–6 | expanded delta authority (occurrence, timeline, media linkage); unrelated-change law; six unrelated-change fixtures | RESOLVED (F3) |
| 7–8 | one canonical commit eligibility entry point (option B); no hash-only success | RESOLVED (F4) |
| 9–10 | journal READBACK_S1 → S1 binding; S1 session consistency | RESOLVED (F5) |
| 11–13 | capability → snapshot enforcement; field provenance model (normalized method map bound by the guard); CANDIDATE vs QUALIFIED observation | RESOLVED (F6) |
| 14–16 | S1 coverage profile law; S0/S1 compatibility; `APPEND_VERIFY` profile | RESOLVED (F7) |
| 17 | composed validator single authority with explicit executable stage sequence (`VALIDATION_STAGES`) | RESOLVED |
| 18 | structural bypass tests (`bypass-audit`: surface classification, docstrings, signatures, behaviour) | RESOLVED |
| 19 | linked artifact identity (authority version, plan digest, transaction id, target, H0/S0 guard, operation-set digest, session, S1 digest, verification digest) across plan/journal/S0/S1/verification/commit | RESOLVED |
| 20 | no v1.4 direct validator remains authorizing; helpers documented INTERNAL_NON_AUTHORIZING and refuse partial input | RESOLVED |
| 21 | all Codex v1.4 attacks + combinations in the suite (`v1.4-attack`) | RESOLVED |
| 22 | seeded randomization of snapshots/occurrences/evidence/linked sets; duplicates reject identically | RESOLVED |
| 23 | two fresh-process runs, deterministic report bytes, zero failures, no cache; per-finding coverage reported | see authoring report |
| 24–25 | branch from exact v1.4 HEAD; history preserved; manifest with parent identity + this matrix; commit; no merge | see authoring report |
| 26 | return format | authoring report |

Not corrected (out of the enumerated scope, unchanged from v1.4): attachment envelope/freshness laws, eligibility codes, permission entries, timebase, track policy, canary manifest, drift/marker policy, transport, doctrine, client version inventory, M3 probe plan.
