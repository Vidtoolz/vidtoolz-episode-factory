# FINDING-RESOLUTION-MATRIX-v1.4.md

Independent review of v1.3: six MAJOR findings and one MINOR, plus mission items 1–25 (Mikko's v1.4 mission). Severity column reflects the review; mission items without a review severity are marked CORRECTION.

## Review findings

| # | Finding | Severity | Correction | Where | Status |
|---|---|---|---|---|---|
| F1 | attachment evidence could be mixed across incompatible authority/target records | MAJOR | every record carries an identity envelope (authority version, manifest sha, host, product/version/build, library name/uuid/root, session id, provisioning id, project/timeline identity, target epoch, sequence, captured_at, record type version); level law BUNDLE/LIBRARY/SESSION; `envelope_conflicts` over the current session → `CONFLICT`; bindings must cite the provisioning record; a record from another target/session never composes | `schemas/resolveEvidenceSet.schema.json`, `TARGET-CONTRACT.json#evidence_envelope_law`, `TARGET-ATTACHMENT-GATE.md`, `authority_lib.envelope_errors/validate_evidence_set/envelope_conflicts/derive_attachment_state`; fixtures `attached-cross-library-timeline-binding`, `attached-second-provisioning-other-uuid`, `attached-binding-other-session`, `invalid-*` | RESOLVED |
| F2 | attachment freshness was insertion-order dependent; stale observations could win | MAJOR | no positional selection anywhere (`records[-1]`/tail removed; validator asserts); `current_record` = highest `envelope.sequence` within `current_session_id`; equal highest sequences → AMBIGUOUS → `CONFLICT`; `captured_at` older than 3600 s vs `evaluated_at` → STALE; sequence/timestamp disorder → `CONFLICT`; other sessions never current; seeded permutation tests prove identical results in any order | `authority_lib.current_record/session_order_errors`, `TARGET-ATTACHMENT-GATE.md`; fixtures `attached-stale-good-current-eka`, `attached-current-good-stale-eka`, `attached-duplicate-sequence-conflict`, `attached-duplicate-timestamp-distinct-sequence`, `attached-connection-previous-session-only`, `attached-ancient-observation`, `attached-sequence-timestamp-disorder`, `attached-reversed-order`; validator `order-independence` | RESOLVED |
| F3 | degraded timeline observations unrepresentable when required timeline fields were unavailable | MAJOR | timeline `field_status` on unique_id, name, start_frame, end_frame, start_timecode, fps, width, height, is_current, settings (plus project and track fields); values nullable; OBSERVED requires value **and** a callable producing primitive; degraded capture forces `complete:false` with reasons; pre-qualification capture defined exactly | `schemas/resolveSnapshot.schema.json`, `SNAPSHOT-COMPLETENESS.md`, `authority_lib.semantic_snapshot`; fixtures `snapshot-degraded-start-frame-*`, `snapshot-m0-minimal-nothing-qualified-honest`, `snapshot-timeline-*`, vector `unobserved_timeline_all_null_with_status` | RESOLVED |
| F4 | capability qualification evidence could be forged or accepted despite failed/stale review evidence | MAJOR | `QUALIFIED_CALLABLE` requires the active matrix row `QUALIFIED_READ` under a reviewed `refreeze` block, an evidence-record entry citing (method, probe_id, raw_evidence_sha256), and a linked `CAPABILITY_EVIDENCE` record that is SUCCESS, exact host/product/version/build, expected receiver, `reviewed: true` + `ACCEPT`, promoted by the active authority version **and** active matrix sha, with `RAW_EVIDENCE` present and re-hashing; failed/unreviewed/rejected/old-refreeze/wrong-host/wrong-build/adjacent-getter/no-raw/tampered-raw all rejected | `CAPABILITIES.json`, `M0-PROBE-CONTRACT.md`, `authority_lib.primitive_status/capability_record_qualifies/semantic_capability_record/semantic_capabilities`; fixtures `attached-evidence-*`, `capabilities-qualified-read-*`, validator `capability-qualification-negative` | RESOLVED |
| F5 | journal APPLIED operation ids need not belong to the bound plan | MAJOR | every operation-bearing record (`OP_STARTED`, `APPLIED`, `OP_FAILED`) must name an `operation_id` of the bound plan with the same `op`; `operation_set_digest` in every record equals the plan's; each op started/applied at most once, APPLIED only after its own OP_STARTED; `READBACK_S1` only after every plan op is APPLIED; non-operation records carry null | `schemas/provisional/resolveTransactionJournal.schema.json`, `SEMANTIC-VALIDATION.md`, `authority_lib.semantic_journal`; fixtures `journal-applied-operation-not-in-plan`, `journal-invented-operation-started-and-applied`, `journal-op-type-inconsistent-with-plan`, `journal-applied-without-started`, `journal-operation-applied-twice`, `journal-readback-before-all-applied`, `linked-set-journal-*` | RESOLVED |
| F6 | verification could claim success without proving the operation effect from S0→S1 | MAJOR | the validator resolves S0 and S1 records (payload digest, guard digest, snapshot object digest, lineage, plan H0), derives the delta (`derive_delta`), derives expected effects (`expected_effects`, provisional APPEND law), computes `verify_transaction` and rejects any declared result that differs from the derived truth; APPEND + VERIFIED + `added: []` + empty creation map is rejected; arbitrary S1 hashes rejected; `EFFECT_NOT_SPECIFIED` for ops without an effect law | `SEMANTIC-VALIDATION.md`, `authority_lib.derive_delta/expected_effects/verify_transaction/semantic_verification_result/validate_transaction_set`; fixtures `verification-*`, `linked-set-append-without-effect`, `linked-set-s1-arbitrary-hash`, validator `append-effect`, `s0-s1-resolution`, `derived-delta` | RESOLVED (provisional effect law) |
| F7 | project identity observation status/reason not in guard authority | MINOR | guard v2 carries project and timeline identity **with** `unique_id_status`, `unique_id_reason`, `name`, `name_status`; UNAVAILABLE ≠ ERROR produces different guard digests over an identical payload | `GUARD-AUTHORITY.md`, `schemas/resolveGuard.schema.json`, `authority_lib.guard_object`; fixture `fixtures/guard/guard-vectors.json`, validator `guard` | RESOLVED |

## Mission items

| # | Item | Correction | Status |
|---|---|---|---|
| 1 | evidence envelope | envelope on every record; level law; coherence fields | RESOLVED (F1) |
| 2 | exact manifest binding | `validate_evidence_set(es, active)`; `BUNDLE_VERIFICATION`/`MILESTONE_EXIT`/`M3_AUTHORIZATION`/`REFREEZE_RECORD` count only when bound to the active manifest + version 1.4.0 and this host; historical bundles tolerated, never current; validator section `exact-manifest` proves it against the real `FREEZE-MANIFEST.json` sha (arbitrary 64-hex, v1.3 manifest, other host, other version, self-verified all rejected) | RESOLVED |
| 3 | coherent library binding | provisioning uuid/root are the reference; launch recipe, connection observation and every binding must cite the same provisioning record, uuid, root and library name; missing root/uuid in the connection observation is `OBSERVED_TARGET_MISMATCH`; second provisioning identity → `CONFLICT` | RESOLVED |
| 4 | freshness / session selection | sequence + session + staleness + explicit AMBIGUOUS; seven listed scenarios are fixtures; permutation property tests | RESOLVED (F2) |
| 5 | contradiction handling | `CONFLICT` state ranks below `UNPROVISIONED`; eligibility short-circuits with `ATTACHMENT_CONFLICT` and the conflict list | RESOLVED |
| 6 | operation-specific attachment | unchanged v1.3 target requirements (CONNECT/ENUMERATE_PROJECTS/probe SESSION; ENUMERATE_TIMELINES PROJECT; SNAPSHOT_CAPTURE PROJECT_TIMELINE); bindings must be CURRENT and coherent; probe = isolated target + exact reviewed authority + `PROBE_ALLOWED_PRIMITIVES`; fixtures prove ENUMERATE_PROJECTS needs no binding records | RESOLVED |
| 7 | timeline observation status model | `TIMELINE_STATUS_FIELDS` (10 incl. the five required); same closed vocabulary; OBSERVED requires value; UNAVAILABLE/ERROR require null + reason | RESOLVED (F3) |
| 8 | degraded snapshot | `GetStartFrame` unavailable → `start_frame: null`, status UNAVAILABLE + reason, `complete: false`, reason recorded, canonicalizable; fixtures positive and three negatives (claimed OBSERVED, value with UNAVAILABLE, claims complete) | RESOLVED |
| 9 | zero qualified reads | `callable_method_set` is empty under the frozen matrix (validator proves 0 callable); first legal M0 op is the probe; SNAPSHOT_CAPTURE before qualification yields only the connection/library degraded capture (`SNAPSHOT-COMPLETENESS.md §Pre-qualification capture`) and is never operationally complete; hypothetical matrix shows the positive path | RESOLVED |
| 10 | capability evidence authenticity | eleven requirements machine-checked; rejection list covered | RESOLVED (F4) |
| 11 | probe evidence | probe output = `CANDIDATE_CAPABILITY_EVIDENCE` (`qualification.reviewed: false`) + `RAW_EVIDENCE` + one `CONNECTION_OBSERVATION`; promotion = independent review decision → capability refreeze (new matrix, reviewed `refreeze` block) → `REFREEZE_RECORD` naming the matrix sha → records' `promoted_by`; `CAPABILITIES.json#qualification_pipeline`; frozen matrix + reviewed evidence → still UNQUALIFIED (validator) | RESOLVED |
| 12 | journal operation membership | see F5; `operation_set_digest` linked into every record | RESOLVED |
| 13 | journal execution law | `PREPARED → LEASED → PREFLIGHT_OK → [CHECKPOINTED] → OP_STARTED → APPLIED | OP_FAILED → READBACK_S1 → VERIFIED → SAVED → PUBLISHED → COMMITTED`, recovery branch; multiplicity rules | RESOLVED |
| 14 | S0/S1 evidence | record exists, hash matches (payload, guard, snapshot object), target/lineage matches plan and S0, observation valid (schema + semantic + WRITE_PRECHECK complete) | RESOLVED (F6) |
| 15 | derive delta | added/removed/changed occurrences, marker add/remove, creation identity mapping, missing expected, unrelated | RESOLVED |
| 16 | APPEND expected effect | ADDED_ITEM on selector track at planned range with expected media identity; rejects no new occurrence, wrong track, wrong range, wrong media, unrelated changes, not APPLIED; nothing beyond `expected_effects` is claimed | RESOLVED (provisional) |
| 17 | composed linked-set validation | `validate_transaction_set`: S0 → plan → journal → S1 → derived delta → expected effect → verification vs derived truth → commit; commit refuses hash-only inputs | RESOLVED |
| 18 | guard observation status | guard v2 | RESOLVED (F7) |
| 19 | randomized-order fixtures | validator `order-independence`: 74 eligibility cases × 4 evidence/request permutations, 54 evidence sets × 4, every snapshot fixture × 4 (tracks/items/markers/ledger), 18 linked sets × 4 (evidence map, conflicts, snapshot arrays, key order), seed 20260909 | RESOLVED |
| 20 | all v1.3 attacks in the suite | validator section `v1.3-attack` maps each independent attack to its fixtures and asserts they fail at the intended layer | RESOLVED |
| 21 | validation run twice in fresh processes | authoring report records both runs and byte-identical reports; count in `VALIDATION-REPORT.md` | see authoring report |
| 22 | versioning | v1.4 bundle + manifest with parent v1.3 head + manifest, changed/inherited flags, finding resolution (this file), new sha; history byte-identical | RESOLVED |
| 23 | commit | branch `docs/resolve-authority-freeze-v1.4`; candidate commit; not merged | see authoring report |
| 24–25 | review handoff / return format | authoring report | see authoring report |

Not corrected (out of the enumerated scope, unchanged from v1.3): timebase law, track policy, canary manifest, drift/marker policy, transport, doctrine, client version inventory, M3 probe plan, permission entries.
