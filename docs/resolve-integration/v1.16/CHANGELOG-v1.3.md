# CHANGELOG — Resolve authority bundle v1.3 (narrowly scoped correction of v1.2)

- **Parent:** v1.2.0, branch `docs/resolve-authority-freeze-v1.2` @ `c6d1284c6f395f5b21a6d14f8c2873bccfc065bb`, manifest `69e1caecf9ba9bd16875b7625c58902e3e5f613372165c8d11d80ff48939b5e6`. Grandparent v1.1.0 @ `47ddb225335b8c85ca5255b1de86ff508e0e8e91` (`83d8a307098cdc18931f6f09de2ce782afa0bdef94ec8540c2a143910cd448c5`); v1.0.0 @ `e2874f6f67f5adb3a5ced0e0e138c370e38995cb` (`d9cd54780e0a123ae2045ac575bbc41638039ef3e878f1b608afbce3989a8821`).
- **This version:** 1.3.0, directory `docs/resolve-integration/v1.3/`, own `FREEZE-MANIFEST.json` (schema `resolveFreezeManifest.v1.3`; sha256 **and byte count** per file, validated by the suite).
- **Rationale:** Codex's independent review of v1.2 (5 MAJOR, 2 MINOR, 0 BLOCKER; four MAJORs affecting M0/M1) as enumerated in Mikko's v1.3 mission (24 items). Only those defects are corrected. Codex's review file was not present on disk; the mission's enumeration is the input of record.
- **Scope discipline:** no Resolve launched or mutated; no M0; no EKA access; no adapter; v1.0/v1.1/v1.2 bytes untouched; unrelated Earth Studio work untouched.

## Changed authorities
- `tools/authority_lib.py` (rewritten reference): content-addressed evidence records (`record_id`, `validate_evidence_set`, `resolve_ref`, `find_records`); `derive_attachment_state`; `primitive_status` (QUALIFIED_CALLABLE / PROBE_ALLOWED / UNQUALIFIED); `evaluate_eligibility(perms, request, rp, caps, tc, evidence_set)` consuming only validated authority; operation-specific target requirements (`_target_binding`); `validate_f64` with `fullmatch`; status-aware item sort key + `observation_failures` ledger; profile-complete `semantic_snapshot` (identity/locks/guard/ledger/incomplete_reasons); `plan_digest_of`, `operation_set_digest`; binding-aware `semantic_mutation_plan`, `semantic_journal(records, plan)`, `semantic_verification_result(vr, plan)`, `semantic_conflict`, composed `semantic_commit_manifest`; `validate_transaction_set`; `semantic_manifest` byte-count equality; 23 prerequisite codes (+`PROBE_ALLOWED_PRIMITIVES`, `TARGET_REQUIREMENT_SATISFIED`).
- `TARGET-CONTRACT.json` (+schema v1.3): `attachment_state`/`attachment_evidence` removed; `attachment_state_is_declared: false`; `attachment_derivation`; `attachment_states` as required record types; `required_future_observations` restated as records.
- `PERMISSIONS.json` (+schema v1.3): `target_requirement` per entry; `TARGET_REQUIREMENT_SATISFIED` on every entry; `PROBE_ALLOWED_PRIMITIVES` on the probe; schema forces M3 + both mandatory codes whenever `mutation_allowed` is true.
- `READ-PRIMITIVES.json` (+schema v1.3): `target_requirement` per logical op; probe flags (`purpose_is_qualification`, `read_only`, `promotes_capability: false`, `evidence_output: CANDIDATE_EVIDENCE`, per-primitive `probe_allowed`).
- `CAPABILITIES.json` (+schema v1.3): `probe_candidate` on read rows; `reviewed_refreeze_version` on evidence records; `probe_law`.
- `MILESTONE-MATRIX.json` (v1.3: target requirements per milestone); `M3-PROBES.json` (P15 rename claim removed, option B).
- `schemas/resolveSnapshot.schema.json` (v1.3: 9 status fields, nullable frames, `observation_failures`, `incomplete_reasons`, payload domain v1.3); `schemas/resolveGuard.schema.json` (coverage incl. `incomplete_reasons`; nullable identity only for read-only profiles).
- Provisional schemas: plan (`transaction_id`, `authority_version`, `operation_set_digest`, `refs`; declared-state/evidence/token fields removed), journal (`target_ref`, `guard_digest`, `authority_version`, `recovery_of_transaction_id`), verification (`transaction_id`, `target`, `h0/s1_guard_digest`, `expected_delta_digest`, `readback_snapshot_sha256`, `authority_version`), conflict (`plan_digest`, `resolved`, `authority_version`), commit (`target`, `authority_version`, `operation_set_digest`).
- New schemas: `schemas/resolveEvidenceSet.schema.json`, `schemas/resolveEligibilityRequest.schema.json`; `schemas/resolveFreezeManifest.schema.json` v1.3 (parent pins).
- Documents: new `M0-PROBE-CONTRACT.md`; rewritten `ELIGIBILITY.md`, `TARGET-ATTACHMENT-GATE.md`, `SNAPSHOT-COMPLETENESS.md`, `SEMANTIC-VALIDATION.md`; edited `CANONICALIZATION.md` (f64 fullmatch, status-aware order, domains, version 1.3), `GUARD-AUTHORITY.md`, `MILESTONES.md` (M0 row), `M3-MATRIX.md` (P15), `SCORECRAFT-EXTRACTION.md` (v1.3 active references), `PROVISIONAL.md`, `README.md`, `AUTHORITY-PRECEDENCE.json/.md` (S11–S17; v1.2 changelog/matrix → HISTORICAL; retired terms).
- Tools: `tools/build_v1_3.py`, `tools/validate_v1_3.py`, `tools/build_manifest.py` (parent v1.2).
- Fixtures: `fixtures/layered/` (136, layers none/schema/semantic/eligibility), `fixtures/evidence/` (24 evidence sets), `fixtures/eligibility/` (50 cases + hypothetical matrix), `fixtures/linked-set/` (13), `fixtures/plan/`, `fixtures/snapshot/` (3), `fixtures/guard/`, `fixtures/canonicalization/` (19 vectors, 36 rejections). Inherited unchanged: `fixtures/parse/`, `fixtures/timebase/`.

## Inherited unchanged
`AUTHORIZATION-2026-09-08.md`, `DOCTRINE.md`, `TRANSPORT.md`, `DRIFT-POLICY.md`, `REVIEW-MARKER-POLICY.md`, `IDENTITY-BINDING.md`, `SNAPSHOT-CONCURRENCY-RECOVERY.md`, `CANARIES.md`, `CLIENT-VERSION-INVENTORY.md`, `TIMEBASE.json` (+schema), `CANARY-SOURCE-MANIFEST.json` (+schema), `schemas/resolveTrackPolicy.*`, `QUARANTINE-MANIFEST.json`, provisional `resolveSequenceLineage`/`resolveBindingSet`/`resolveBindingObservation`/`resolveCheckpoint`, historical inputs.

## Removed relative to v1.2
`tools/build_v1_2.py`, `tools/validate_v1_2.py` (replaced); v1.2 `fixtures/layered/*`, `fixtures/eligibility/cases.json`, `fixtures/canonicalization/*`, `fixtures/snapshot/*`, `fixtures/guard/*` (regenerated under v1.3 rules); `VALIDATION-REPORT.md` regenerated.

## Mutation prohibition preserved
No Resolve launched or mutated; no EKA; no M0 begun; no adapter implementation; mutation schemas remain PROVISIONAL_UNTIL_M3; `mutation_allowed` only at M3 with `MIKKO_M3_AUTHORIZATION` and `TARGET_STATE_SCRATCH_WRITE_READY`; at v1.3 no read primitive is callable outside the probe (zero QUALIFIED_READ rows).
