# MILESTONES.md — exact M0/M1/M2/M3 boundaries (v1.7, FROZEN_NOW)

Permission declarations: `PERMISSIONS.json` (default DENY). Eligibility: `ELIGIBILITY.md`. Machine cross-check: `MILESTONE-MATRIX.json`. `SCHEMA-VALID != AUTHORIZED TO MUTATE`. No milestone below is begun by this bundle.

| M | Entry gate | Work allowed | Exit evidence | Prohibited |
|---|---|---|---|---|
| **M0 — read-only connection and observation, in four phases (`M0-PHASES.md`)** | bundle independently hash-verified against the exact active manifest (`BUNDLE_VERIFICATION` bound to this `FREEZE-MANIFEST.json` sha and authority 1.20.0, verifier ≠ preparer, this host); derived target state `ATTACHMENT_READY` (`PROVISIONING_RECORD`, pinned `LAUNCH_RECIPE`; `TARGET-ATTACHMENT-GATE.md`); every operation carries its `target_requirement` (SESSION / PROJECT / PROJECT_TIMELINE, `ELIGIBILITY.md`); PRESTO/VIDLAP2 versions recorded or explicitly deferred | **M0A** `READ_PRIMITIVE_QUALIFICATION_PROBE` (first and, at v1.6, the only session-establishing operation; SESSION scope; evidence bound to the exact active manifest; calls each of the 47 allowlisted getters through the capture shim of `CAPTURE-SHIM.md` and records one `RAW_CAPABILITY_CAPTURE` per attempt — facts only, no interpretation — plus `CONNECTION_OBSERVATION` and the identity read passes; a `BINDING_MISMATCH` (family `FATAL_TARGET_FAILURE`) stops it; promotes nothing). **M0B** offline: re-parse every capture with the reference parser and record one `REVIEW_DECISION` each (reviewer ≠ operator). **M0C** Mikko-approved capability refreeze in a successor bundle. **M0D** once the matrix is active: `CONNECT`, `TIMEBASE_OBSERVE` and full `SNAPSHOT_CAPTURE` become eligible. Between M0A and M0C, once the connection observation derives `ATTACHED_READ_ONLY`: `ENUMERATE_PROJECTS` (SESSION), `ENUMERATE_TIMELINES` (PROJECT binding), `SNAPSHOT_CAPTURE` (PROJECT_TIMELINE binding; **only** the pre-qualification capture of `SNAPSHOT-COMPLETENESS.md` — connection/library domains, every project/timeline field UNAVAILABLE, no tracks, `complete:false` — which is never operationally complete and never a plan H0), `TRIPWIRE_READ` (PROJECT, idle only); `CONNECT` and `TIMEBASE_OBSERVE` have REFUSE fallbacks and are **not eligible** before M0D; unqualified primitives are never called silently | `M0-EVIDENCE/` (`EVIDENCE-ROOT.md`, append-only, finalized and hashed): connection, launch-recipe sha, one capture per getter attempt, identity uniqueness and stability observations, ≥2 consecutive snapshots with equal guard digests per target, read-only journal proving zero mutators, report → M0B review of every capture → M0C capability refreeze in a successor bundle (rows `QUALIFIED_READ` citing raw/derived/review digests) → `REFREEZE_RECORD` kind `M0_READ_REQUALIFICATION` naming the parent and successor matrix content digests, the parser, the primitive spec and a human approver (`M0-PROBE-CONTRACT.md`, `REVIEW-REFREEZE.md`) | any mutator incl. `SaveProject`, `CreateEmptyTimeline`, `ImportMedia`, markers, settings; `EKA`; user's Local Database; Mikko's live session; any classification written by the probe |
| **M1 — executable contracts (offline)** | M0 exit evidence | Node canonicalization reproducing all vectors and rejections; executable validators; timebase implementation reproducing the 21-boundary fixture; binding-set generation from the canary (dry-run); occurrence identity resolver over M0 snapshots; read-only mechanical QC; journal/recovery state machine against fixtures | vectors/fixtures pass in Node and Python; resolver behaves on ≥3 synthetic snapshots; recovery reaches a terminal state for every injected fault | Resolve calls beyond re-reading M0 targets (read ops require `M0_EXIT_EVIDENCE`) |
| **M2 — read-only attach and dry-run** | M1 exit | attach collector (`ATTACHED_READ_ONLY`); Canary C1; `SetCurrentTimeline` on adapter-prefixed projects only; `BUILD_MUTATION_PLAN_DRY_RUN` against a real guard; comparator on an operator-made change; pre-write revalidation detecting the change with zero mutators; PREPARED records never executed | C1 pass; comparator/revalidation detect the human change; dry-run plan passes schema + semantic layers | any mutator; `SaveProject`; canary media |
| **M3 — scratch qualification (first mutation-capable)** | M2 exit; Mikko's explicit M3 authorization token; target `SCRATCH_WRITE_READY`; the H0/S0 early write gate of `ELIGIBILITY.md` satisfied **before** any mutator (complete `WRITE_PRECHECK` guard record with qualified provenance, active matrix and the plan target); PREPARED journal before every mutator and a mandatory `CHECKPOINTED` record with checkpoint evidence before the first one; commit only via `commit_eligibility_authorizing` (internally resolves the pinned schema validator; no caller-supplied validator) over the full linked set (S0 `WRITE_PRECHECK`, plan, journal with `READBACK_S1` naming S1, S1 `APPEND_VERIFY` or stronger, derived verification incl. `applied_operation_ids` and `unobserved_domains`) | the 19 probes P1–P19 of `M3-MATRIX.md`/`M3-PROBES.json` on disposable `VIDTOOLZ_RESOLVE_QUAL_V1_*` projects with synthetic media; **scratch lifecycle** narrowly permitted: `CreateProject`, `CreateEmptyTimeline`, `SaveProject`, `LoadProject_adapterPrefixed` | per-probe evidence; the next major bundle version refreeze per the refreeze law | canary media; `EKA`; user's Local Database; non-prefixed project; `SetCurrentDatabase`; ripple delete; `ReplaceClip`; render; any M4+ operation |

Later milestones remain as described in the historical adjudication; none is authorized here. `MUTATION_AUTHORIZED` is unreachable before the next major bundle version plus Mikko's activation.
