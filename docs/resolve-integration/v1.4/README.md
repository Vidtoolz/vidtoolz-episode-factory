# Resolve execution subsystem: authority bundle v1.4

Versioned, narrowly scoped correction of v1.3 (parent branch `docs/resolve-authority-freeze-v1.3` @ `1c9e090fcc8978e6e9b8f5dd538849448044d26b`, parent manifest `ad1e6bfcbcb41d79c230795d64e031438e8e39a194f640daca68532d798a55dc`; grandparent v1.2 `c6d1284` / `69e1caec…b5e6`; v1.1 `47ddb22` / `83d8a307…48c5`; v1.0 `e2874f6` / `d9cd5478…8821`). `../v1/`, `../v1.1/`, `../v1.2/` and `../v1.3/` are immutable history. Read `FREEZE-MANIFEST.json` first (closed status vocabulary per file; sha256 and byte count per file), then `AUTHORITY-PRECEDENCE.md`, `CHANGELOG-v1.4.md` and `FINDING-RESOLUTION-MATRIX-v1.4.md`. `PROVISIONAL.md` lists what is not frozen.

Laws in one line each: `SCHEMA-VALID != AUTHORIZED TO MUTATE` (`SEMANTIC-VALIDATION.md`); `PERMISSION DECLARATION != ELIGIBILITY` (`ELIGIBILITY.md`); `PROBE_ALLOWED != QUALIFIED_READ` and `CANDIDATE_CAPABILITY_EVIDENCE != QUALIFIED_READ` (`M0-PROBE-CONTRACT.md`); attachment state is derived from a **coherent, current, envelope-bound** evidence set against the **active reviewed manifest**, never declared, and contradictory evidence is `CONFLICT` (`TARGET-ATTACHMENT-GATE.md`); every timeline/project/track/item field carries an observation status and a degraded capture is always representable (`SNAPSHOT-COMPLETENESS.md`); verification is **derived** from S0 and S1, never declared (`SEMANTIC-VALIDATION.md`).

Validate with `python3 -B tools/validate_v1_4.py` (report: `VALIDATION-REPORT.md`; layers raw parse → schema → evidence-binding → attachment → capability → snapshot → semantic → eligibility → linked-set; seeded random-order property tests; exact-manifest binding against the real `FREEZE-MANIFEST.json`). Regenerate machine artifacts with `python3 -B tools/build_v1_4.py` from v1.3-seeded inputs, then the manifest with `python3 -B tools/build_manifest.py` after a passing validation, then validate again (two cycles converge; the report never embeds the manifest sha). Run every tool with `python3 -B`; a `__pycache__` directory is a validation failure.

Nothing in this bundle is human approval of any run, gate or publication; no Resolve was launched or mutated; `EKA` was not accessed; M0 has not begun; no adapter exists; mutation-capable schemas remain PROVISIONAL_UNTIL_M3; the capability matrix still has zero `QUALIFIED_READ` rows.
