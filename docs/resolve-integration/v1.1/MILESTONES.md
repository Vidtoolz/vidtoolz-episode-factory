# MILESTONES.md — exact M0/M1/M2/M3 boundaries (v1.1, FROZEN_NOW)

Permission is machine-readable in `PERMISSIONS.json` (default DENY). This file is the human-readable boundary. `SCHEMA-VALID != AUTHORIZED TO MUTATE` throughout.

| M | Entry gate | Work allowed | Exit evidence | Prohibited |
|---|---|---|---|---|
| **M0 — connection, host, read-only observation** | bundle v1.1 independently hash-verified; `TARGET-CONTRACT.library.provisioning_status = PROVISIONED` (operator provisions the qualification library and isolated session under separate authorization); PRESTO/VIDLAP2 versions recorded or explicitly deferred | launch the isolated qualification Resolve by the recorded recipe; connect; `GetVersionString`, `GetCurrentDatabase`, project/timeline/track/item enumeration on an operator-created scratch project and on an operator-imported `.drp` copy of a real project (never `EKA`); capture snapshots (≥2 consecutive per target) under `resolveSnapshot.v1.1`; observe `GetStartFrame/GetStartTimecode/GetSettings` on an operator-made 01:00:00:00 @ 30 timeline; exercise every DOCUMENTED_NOT_QUALIFIED *read* row; read `GetProjectLastModifiedTime` across idle time only | `M0-EVIDENCE/`: connection.json, launch-recipe sha, snapshots with equal consecutive guard digests, `capabilities-v2-read-rows.json`, timebase-observation.json, read-only journal, M0-REPORT.md; collector completes on the human-project copy without schema failure | any mutator including `SaveProject`, `CreateEmptyTimeline`, `ImportMedia`, markers, settings writes; `EKA`; user's Local Database; Mikko's live session |
| **M1 — executable contracts (offline)** | M0 exit | Node canonicalization reproducing all vectors byte-for-byte; executable validators for frozen schemas; timebase implementation reproducing the 21-boundary fixture; binding-set generation from the canary handoff (dry-run, no Resolve); occurrence identity resolver over M0 snapshots (0/1/many; `TARGET_MISSING`, `IDENTITY_AMBIGUOUS`); read-only mechanical QC over snapshots; journal/recovery state machine against fixtures with fault injection | vectors and fixtures pass in Node and Python; canary binding set = 20 BEAT_LAYER + 2 PROGRAMME_AUDIO rows with fixture frames; resolver behaves on ≥3 synthetic snapshots; recovery reaches a terminal state for every injected fault | any Resolve call beyond re-reading M0 targets |
| **M2 — final pre-mutation (read-only attach + dry-run)** | M1 exit | attach collector to the qualification session; Canary C1 readback determinism; build a full `resolveMutationPlan` (dry_run true) for one synthetic binding against a real H0/guard; deterministic diff of two real snapshots differing by an operator-made change → comparator reports exactly that item as `unrelated` / `HUMAN_CONFLICT`; pre-write revalidate against an operator-changed timeline → `SNAPSHOT_CONFLICT` with zero mutators; journal PREPARED records written and never executed; recovery planner over synthetic partial journals | C1 pass; comparator and revalidation detect the human change; dry-run plan validates (schema + semantic); journal durability tests pass | `SetCurrentTimeline` only on adapter-prefixed projects; no mutator; no `SaveProject`; no canary media |
| **M3 — scratch qualification (first mutation-capable)** | M2 exit; Mikko's explicit M3 authorization; PREPARED journal before every mutator | the 18 probes of `M3-MATRIX.md` on disposable `VIDTOOLZ_RESOLVE_QUAL_V1_*` projects with synthetic media in the qualification library only; **scratch lifecycle** narrowly permitted: `CreateProject`, `CreateEmptyTimeline`, `SaveProject`, reopen the same adapter-prefixed project (`LoadProject_adapterPrefixed`) for P15/P16 | per-probe `probe-<n>.json` with S0/S1 guard digests, journal, observation, PROVES/DOES-NOT-PROVE; bundle **v2.0.0** refreeze per `REFREEZE-LAW` in `M3-MATRIX.md` | canary media; `EKA`; user's Local Database; any non-prefixed project; `SetCurrentDatabase`; ripple delete; `ReplaceClip`; render; any M4+ operation; treating a probe pass as qualification of anything the probe did not exercise |

Later milestones (M4 single-binding materialization … M13 rollout) are described in `ADJUDICATION-FREEZE-CONTRACT.md` §10 and `AUTHORITY-ARCHITECTURE-ADJUDICATION-M0-M3.md`; none is authorized by this bundle. `MUTATION_AUTHORIZED` is unreachable before bundle v2.0.0 plus Mikko's activation.
