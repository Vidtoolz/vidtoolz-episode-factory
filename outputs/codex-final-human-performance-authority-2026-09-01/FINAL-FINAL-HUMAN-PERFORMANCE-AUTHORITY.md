# CERTIFIED — FINAL HUMAN PERFORMANCE AUTHORITY COMPLETE

## 1. Verdict

CERTIFIED — FINAL HUMAN PERFORMANCE AUTHORITY COMPLETE.

## 2. Authoritative baseline

Live verification found `main = origin/main = bccecb16f0c512f31dca76389d43519b9a0a360f`, tree `06363a411e896f768e43276768c393d29c55b09b`, and 236 dirty entries. Work was performed in `/home/vidtoolz/work/final-human-performance-authority` on branch `codex/final-human-performance-authority`; main and sibling Earth Studio WIP were preserved.

## 3. Existing Presenter Take authority

Presenter Take Manifest V1 remains canonical for reusable presenter-take identity/provenance primitives and its 116/116 tests remain green. It already supplied Story/unit binding, media hash/probe validation, immutable successor manifests, human-only selection, technical/creative separation, and Editor projection. Its unit-scoped shape could not express one whole-script file plus a Final section composite, so `scripts/final-performance.js` is a lock/package-bound Final adapter; no competing generic `finalPerformanceTake.v1` authority was created.

## 4. Final performance doctrine

Normal Draft narration remains synthetic. The real r2 Mikko voice is explicitly a Draft exception and has no Final authority. Final Production requires a fresh lock-bound Mikko recording, explicit selection, and complete coverage.

## 5–10. Ingest, identity, selection, coverage

`node scripts/final-performance.js ingest --run-id <run> --file <recording>` resolves the lock, package, script, Story and presenter itself. MOV, MP4, M4A, WAV and AIFF-family inputs use ffprobe plus SHA-256; canonical media is copied into the run and preserved. Each take records source/canonical hashes, bytes, duration, streams, dimensions/audio properties, provenance, coverage declaration and technical state. Identical re-ingest is `ALREADY_REGISTERED`.

Selection is human-only and requires `Mikko Pakkala`; machine/auto/agent/model/tool selectors are refused. Whole-script selection maps one declared whole-script take to every section. Section selection maps only named sections, allowing composites. Coverage is explicit; duration never infers sections. Completion requires current lock/package/manifest, valid selected bytes, valid human authority, and every required section covered.

## 11–13. Composite, re-selection, rejection

Fixture certification passed for full take, multi-take composite, incomplete coverage, successor re-selection, and rejected-take preservation. Re-selection creates an immutable successor manifest and leaves prior selection history intact. Rejected takes remain stored, cannot cover sections, and are not deleted.

## 14. Stale authority

Lock, package, performance-package, manifest, selected-media path/hash, script, and section-binding drift fail closed with typed errors. Old manifests/projections remain historical and do not become current after a successor.

## 15. Resolve projection

The derived projection writes `PERFORMANCE_<section>` mappings to selected take ID, canonical path, SHA-256 and duration. Incomplete sections remain unresolved. Historical projections are immutable by manifest; the canonical `final-resolve-blueprint.json` is never mutated, and no final edit is created.

## 16. Independent lanes

Final Visual Asset Production and Final Human Performance Production are independently actionable after the Final Production Lock. The next-action projection preserves the visual queue while exposing performance tasks.

## 17. Hostile matrix

20 hostile categories are represented in the durable matrix and focused tests: missing/wrong/stale lock or package, corrupt/no-audio/zero media, forged hashes/paths/bindings, duplicate ingest, Draft synthetic and r2 non-escalation, machine/fabricated selectors, unregistered/rejected takes, incomplete/duplicate coverage, stale successor history, Resolve blueprint mutation, final-edit/QC/publication escalation. Focused hostile cases passed.

## 18. Real canary

The real run reports 11 performance sections, 0 final takes, 0 selected takes, completion false, and next action `Mikko records final performance`. No r2 media was ingested.

## 19. Real-run non-mutation

The complete recursive run inventory used 144 files before and 144 after. Every path and SHA-256 matched; the proof is in `REAL-RUN-FULL-SHA256-BEFORE.txt` and `REAL-RUN-FULL-SHA256-AFTER.txt`.

## 20. Clean-worktree verification

The clean isolated candidate passed `./scripts/verify.sh`: `6193/6193 tests passed`, zero failures, plus canonical spec sync and docs authority checks. The first environment attempt was blocked by the local Script Builder checkout being at `28423c7`; the pinned `b4627a8` checkout was then used. Dirty-main output was not used as certification evidence.

## 21. Final Visual regression

Final Visual Asset Production, Directed Draft, V2/V3 successor, Stable-Audio-first Draft music, Final Production Package, Presenter Take Manifest V1, and related regressions remain green within the 6193-test clean run.

## 22. Publication boundary

No final edit, final QC pass, publication approval, or publication artifact is created or authorized. Final music remains out of scope and required.

## 23. Source control

Implementation commit: `ab93debc7e9c502c28fb1c280022c5298b7509b2`; certification/evidence commits: `36c749649e221bb779c11ac027f193e7af00adb4`, `6e96025ed8cb58e108255efbd756d22ad5fdb0c0`; branch tip tree `c208aad411ed8b50904cc8c092fe807cc46179fc`. Merge: no. Push: no. Promotion: no.

## 24. Next human action

A. Generate/select Final visual assets remains actionable.

B. Record the Final human performance when convenient; then ingest and explicitly select it. Visual completion is not a prerequisite for performance ingest.

## 25. Promotion decision

SAFE TO PROMOTE — FINAL HUMAN PERFORMANCE AUTHORITY CERTIFIED.

## 26. Final decision

PROCEED TO PROMOTION. READY FOR FIRST REAL FINAL PERFORMANCE INGEST.
